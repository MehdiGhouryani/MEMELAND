# بررسی و طرح — ردیابی قیمت توکن (ورود + قیمت لحظه‌ای با انقضا)

وضعیت: **بررسی — پیاده‌سازی نشده.** طبق خواسته‌تان، فقط تحلیل و طرح.

---

## ۱. چیزی که همین الان در پروژه هست (زمینه‌ی واقعی، نه فرضی)

با بررسی کد فعلی، این تصویر به‌دست آمد:

### ستون‌های دیتابیس (از قبل موجودند)
جدول `signals` در `site.db` از قبل این‌ها را دارد:
```
contract_address TEXT
chain            TEXT
entry_price      REAL
entry_alert_sent INTEGER DEFAULT 0   -- فقط برای آلارم یک‌باره‌ی رسیدن به ورود
```
**هیچ ستونی برای «قیمت فعلی» یا «تاریخ آخرین آپدیت قیمت» وجود ندارد.** باید اضافه شود.

### سرویس قیمت (از قبل کار می‌کند)
`services/price_feed.py` — دو منبع:
1. **GeckoTerminal** (اصلی) — رایگان، بدون کلید API، تا ۳۰ آدرس در هر درخواست (مستند شده)
2. **DexScreener** (پشتیبان) — وقتی GeckoTerminal جواب نداد

هر دو **رایگان و بدون نیاز به کلید API** هستند — نیازی به هزینه یا ثبت‌نام جدید نیست.

تابع کلیدی: `detect_chain_and_price(contract_address)` — فقط با آدرس کانترکت،
شبکه (chain) را حدس می‌زند و قیمت لحظه‌ای را برمی‌گرداند. **این دقیقاً همان چیزی
است که برای «قیمت لحظه‌ی ثبت سیگنال» لازم دارید** — از قبل نوشته شده، فقط هیچ‌جا
صدا زده نمی‌شود.

### جاب زمان‌بندی‌شده (از قبل در حال اجراست)
`check_entry_alerts` هر **۴۵ ثانیه** اجرا می‌شود، قیمت لحظه‌ایِ سیگنال‌های واجدشرایط
را می‌گیرد (دقیقاً با `get_current_prices` که خروجی batch از GeckoTerminal/DexScreener
می‌دهد)، ولی این قیمت را **فقط برای یک مقایسه‌ی آنی استفاده و بعد دور می‌ریزد** —
هیچ‌جا ذخیره نمی‌شود. زیرساخت «هر چند دقیقه قیمت بگیر» از قبل هست؛ فقط ذخیره‌سازی
و منطق انقضا وجود ندارد.

### رابط کاربری (کاملاً خالی از این اطلاعات)
فرم ثبت سیگنال در وب‌اپ فقط `contract_address` را می‌گیرد (فیلد «CA»). نه در فرم
ثبت، نه در کارت لیست، نه در صفحه‌ی جزئیات — **هیچ‌جا `entry_price` یا قیمت لحظه‌ای
نمایش داده نمی‌شود.** فقط یک دکمه‌ی «کپی آدرس کانترکت» هست.

### محدودیت مهم: فقط سیگنال‌های وب‌اپ
`contract_address` فقط از فرم وب‌اپ قابل‌ثبت است — سیگنال‌های بومیِ ربات (`/fullsignal`)
اصلاً این مفهوم را ندارند. یعنی این قابلیت **فقط برای سیگنال‌های ثبت‌شده از وب‌اپ**
کار می‌کند، مگر یک روز فرم ربات هم به‌روزرسانی شود.

---

## ۲. پاسخ به سه سؤال طراحی‌ای که مطرح کردید

### ۲.۱ «قیمت موقعی که سیگنال داده شده»

**پیشنهاد:** لحظه‌ی ثبت `contract_address` (چه در ساخت سیگنال، چه اگر بعداً با ویرایش
اضافه شود)، `detect_chain_and_price` صدا زده شود و نتیجه در `entry_price` + `chain`
ذخیره شود — دقیقاً همان لحظه، نه با تأخیر.

**⚠️ نکته‌ی مهم UX:** این یک فراخوانی شبکه‌ای است (تا ۱۰ ثانیه timeout). اگر داخل
همان درخواست HTTP ثبت سیگنال انجام شود، دکمه‌ی «ثبت سیگنال» ممکن است چند ثانیه
معلق بماند — تجربه‌ی بدی است. **باید async/بی‌درنگ باشد:** سیگنال فوراً ثبت و به
کاربر نمایش داده شود؛ قیمت ورود در پس‌زمینه (طی چند ثانیه تا حداکثر یک چرخه‌ی جاب)
پر می‌شود. اگر شبکه هیچ قیمتی برنگرداند (توکن خیلی تازه، یا در دیتابیس این دو
منبع نیست)، سیگنال بدون قیمت ورود می‌ماند — مثل امروز، بدون کرش.

### ۲.۲ «قیمت فعلی، هر چند دقیقه آپدیت»

**پیشنهاد:** جاب موجود (`check_entry_alerts`، هر ۴۵ ثانیه) به‌جای اینکه فقط برای
«آلارم رسیدن به ورود» قیمت بگیرد، دامنه‌اش گسترش یابد به **همه‌ی سیگنال‌های باز
(`outcome_status='open'`) با `chain`+`contract_address`، در پنجره‌ی زمانیِ هنوز
فعال** (بند ۲.۳ پایین). قیمت گرفته‌شده در دو ستون جدید ذخیره می‌شود:

```sql
current_price     REAL
price_updated_at  TEXT   -- ISO UTC، دقیقاً مثل بقیه‌ی ستون‌های تاریخ پروژه
```

هر ۴۵ ثانیه ممکن است زیاد باشد برای این منظور خاص (آلارم ورود به تأخیر حساس است،
نمایش UI نه چندان) — می‌شود یک بازه‌ی جداگانه (مثلاً هر ۳-۵ دقیقه) برای آپدیت
«قیمت نمایشی» گذاشت تا هم بار درخواست کمتر شود، هم با آلارم ورود قاطی نشود.

### ۲.۳ «باید تا یک‌جا آپدیت بشه، نه تا ابد»

این مهم‌ترین تصمیمی است که باید بگیرید — چند گزینه، با مزایا/معایب:

| گزینه | توضیح | مزیت | عیب |
|---|---|---|---|
| **الف) پنجره‌ی زمانی ثابت** | مثلاً ۴۸ ساعت بعد از ثبت، آپدیت متوقف شود | ساده، قابل‌پیش‌بینی | سیگنالی که هنوز باز است ولی قدیمی‌تر از ۴۸ساعته، قیمتش فریز می‌شود حتی اگر هنوز «باز» باشد |
| **ب) تا زمان بسته‌شدن نتیجه** | وقتی ادمین/کالر نتیجه را win/loss کرد، آپدیت متوقف شود | منطقی‌ترین از نظر معنایی (چرا قیمت سیگنالِ بسته‌شده را دنبال کنیم؟) | اگر کسی هیچ‌وقت نتیجه را نبندد، تا ابد آپدیت می‌شود |
| **ج) ترکیبی (پیشنهاد من)** | هرکدام زودتر: بسته‌شدن نتیجه **یا** یک سقف زمانی (مثلاً ۷ روز) | جلوی هر دو حالت افراطی را می‌گیرد | یک پارامتر بیشتر برای تنظیم |

**پیشنهاد من: گزینه‌ی ج، با سقف ۷ روز.** اکثر سیگنال‌های میم‌کوینی در همین پروژه
(بر اساس برچسب‌های امتیازدهی `win_2x`/`win_5x`/`win_10x` که دیدم) احتمالاً ظرف
روزها نه هفته‌ها به نتیجه می‌رسند؛ ۷ روز سقفی امن است که بار سرور را هم محدود
نگه می‌دارد.

### ۲.۴ «تاریخ قیمت باید ذکر بشه»

`price_updated_at` ذخیره می‌شود و در UI به‌صورت نسبی نمایش داده می‌شود («۳ دقیقه
پیش» / «۲ ساعت پیش»)، دقیقاً با همان الگوی JS که برای زمان‌های دیگر پروژه به کار
می‌رود. اگر آپدیت متوقف شده باشد (بند ۲.۳)، باید صریح گفته شود («آپدیت قیمت
متوقف شده — آخرین‌بار ۵ روز پیش») تا کاربر گمراه نشود که این عدد زنده است.

---

## ۳. طرح UI/UX پیشنهادی

بر پایه‌ی الگوهای بصری موجود در همان صفحه (بج‌های `roi-badge`، رنگ‌های
`--green`/`--red`، فونت `mono` برای اعداد):

### در کارت لیست (جای بج «نتیجه» فعلی)
وقتی سیگنال باز است و قیمت لحظه‌ای موجود است، به‌جای متن ثابت «در حال معامله»:

```
🟢 +34.2%     ← جایگزین «در حال معامله»، رنگ سبز/قرمز مثل win/loss فعلی
```

اگر آپدیت قیمت متوقف شده یا اصلاً CA ندارد، همان «در حال معامله»ی فعلی می‌ماند —
هیچ رگرسیونی برای سیگنال‌های بدون CA رخ نمی‌دهد.

### در کارت جزئیات (زیر همان بخش آدرس کانترکت که الان هست)
```
┌──────────────────────────────────────────┐
│  قیمت ورود          قیمت لحظه‌ای          │
│  $0.00004120        $0.00005530  🟢+34.2% │
│                      ۳ دقیقه پیش          │
└──────────────────────────────────────────┘
```
اگر پیگیری متوقف شده:
```
│  قیمت ورود          آخرین قیمت ثبت‌شده     │
│  $0.00004120        $0.00005530           │
│                      ۵ روز پیش · آپدیت متوقف شد │
```

این طراحی از الگوی موجود پروژه (کارت‌های دوستونه، فونت مونو برای اعداد، رنگ
بر اساس مثبت/منفی) پیروی می‌کند — چیز جدیدی به زبان بصری اضافه نمی‌کند.

---

## ۴. جنبه‌های فنی که باید حساب شوند

### مقیاس درخواست‌ها
با batch سایز ۳۰ در هر درخواست، اگر مثلاً ۱۰۰ سیگنال هم‌زمان در «پنجره‌ی فعال»
باشند، هر چرخه‌ی جاب حدود ۴ درخواست batch (به‌ازای هر شبکه‌ای که سیگنال‌ها رویش
پخش‌اند) می‌زند — با فاصله‌ی هر چند دقیقه، کاملاً در محدوده‌ی امن است. نیازی به
نگرانی مقیاس در حجم فعلی این پروژه نیست.

### توکن‌هایی که هیچ منبعی قیمتشان را ندارد
میم‌کوین‌های خیلی تازه یا کم‌حجم ممکن است در GeckoTerminal/DexScreener نباشند.
سیستم باید این را با ظرافت مدیریت کند: نه خطا، فقط «قیمت لحظه‌ای در دسترس نیست»
در UI (مثل امروز که `get_current_prices` سکوت می‌کند اگر جوابی نگیرد).

### همخوانی با فیچر آلارم موجود
`check_entry_alerts` و این فیچر جدید هر دو از `get_current_prices` استفاده
می‌کنند. باید تصمیم گرفت: یک جاب مشترک (که هم آلارم می‌دهد هم current_price را
ذخیره می‌کند، برای صرفه‌جویی در تعداد درخواست‌ها)، یا دو جاب جدا با بازه‌های
متفاوت؟ **پیشنهاد: یک جاب مشترک**، چون داده‌ی خام (قیمت لحظه‌ای) دقیقاً همان
چیزی است که هر دو نیاز دارند — فقط منطق پایین‌دستی‌شان فرق دارد.

### سیگنال‌های بومیِ ربات
همان‌طور که در بند ۱ گفته شد، این فیچر فقط برای سیگنال‌های وب‌اپ (که CA دارند)
کار می‌کند. اگر می‌خواهید کالرهای داخل چت هم بتوانند CA بدهند، آن یک فاز جدا و
بزرگ‌تر است (فرم `/fullsignal` باید یک مرحله‌ی «آدرس کانترکت (اختیاری)» بگیرد).

---

## ۵. فازبندی پیشنهادی (اگر تصمیم به پیاده‌سازی گرفتید)

| فاز | محتوا | ریسک |
|---|---|---|
| ۵.۱ | دو ستون جدید (`current_price`, `price_updated_at`) + گرفتن قیمت ورود async هنگام ثبت سیگنال با CA | کم |
| ۵.۲ | گسترش جاب موجود برای آپدیت دوره‌ای + منطق انقضا (بند ۲.۳) | کم — فقط افزودن به جاب موجود |
| ۵.۳ | UI: بج زنده در کارت لیست + پنل قیمت در جزئیات | کم — فقط نمایشی، هیچ منطق بک‌اندی جدید ندارد |
| ۵.۴ | (اختیاری) گرفتن CA از داخل ربات هم | متوسط — فرم چندمرحله‌ای جدید در ربات |

تخمین ۵.۱ تا ۵.۳: در حدود ۳-۴ ساعت کار، با تست کامل.

---

## ۶. سؤالاتی که قبل از شروع پیاده‌سازی لازم است جواب بدهید

۱. **سقف زمانی پیشنهادی (۷ روز، گزینه‌ی ج در بند ۲.۳) قبول است، یا عدد دیگری
   می‌خواهید؟**
۲. **بازه‌ی آپدیت «قیمت نمایشی» چند دقیقه باشد؟** (پیشنهاد: هر ۳-۵ دقیقه — جدا از
   آلارم ورود که همان ۴۵ ثانیه می‌ماند)
۳. آیا می‌خواهید وقتی پیگیری قیمت متوقف می‌شود (سقف زمانی رسید)، یک اعلان به
   ادمین/کالر برود («این سیگنال دیگر ردیابی نمی‌شود، نتیجه را دستی ثبت کنید»)؟
   یا فقط بی‌صدا در UI مشخص شود؟
۴. فاز ۵.۴ (گرفتن CA از داخل ربات) را می‌خواهید همین حالا در نظر بگیریم، یا فعلاً
   فقط وب‌اپ کافی است؟
