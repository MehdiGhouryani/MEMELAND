# گزارش بررسی و اصلاح MEMELAND

تاریخ: ۲۰ سپتامبر ۲۰۲۶ · نسخه‌ی بیلد: `2026-09-20.1`

---

## ۱. علت ریشه‌ای دو مشکل گزارش‌شده

هر دو مشکل — «وب‌اپ منو ادمین نمی‌شناسه» و «سیگنال‌ها نشون داده نمی‌شن» — **یک علت واحد** دارند.

### باگ

```js
const API = { ... };     // api.js
const Views = { ... };   // views.js
const TGBridge = { ... };// telegram.js
const App = { ... };     // app.js
const Modals = { ... };  // modals.js
const Dossier = { ... }; // dossier.js
```

در یک **classic script** (بدون `type="module"`)، اعلان `const`/`let` در بالاترین سطح فقط یک
binding *لغوی* در global scope می‌سازد و **هرگز به `window` اضافه نمی‌شود**. این با `var`
فرق دارد.

نتیجه: `API` کار می‌کرد، ولی `window.API` همیشه `undefined` بود — و کل `app.js` قبل از هر
فراخوانی دقیقاً `window.API` را چک می‌کند:

```js
if (window.API && typeof API.authenticateWebApp === 'function') { ... }
```

### زنجیره‌ی دقیق تا علائم

```
window.API === undefined
   └─> گارد authenticateWebApp رد می‌شود
          └─> هیچ درخواست احراز هویتی به سرور زده نمی‌شود
                 └─> session = null
                        ├─> isAdmin = false  ⇒  پنل ادمین + FAB + بج ADMIN مخفی
                        └─> گارد getSignals هم رد می‌شود
                               └─> state.signals = []  ⇒  total=0
```

### شواهد در لاگ خودتان

```
[BOOT] guardFail check=API.getSession       hasAPI=false
[BOOT] guardFail check=API.authenticateWebApp hasAPI=false
[BOOT] guardFail check=TGBridge.init        hasTGBridge=false
[BOOT] guardFail check=window.Views
[APP]  ready sessUid=none tgUid=2088114041 adm=false sigCount=0
```

`tgUid` پر است ولی `sessUid` خالی — یعنی SDK تلگرام سالم لود شده و آیدی را می‌دهد،
ولی هیچ‌وقت سشنی از سرور گرفته نشده.

**امضای قطعی:** در لاگ شما `avatars.js` و `pull_refresh.js` هیچ‌وقت `guardFail` ندادند —
و اتفاقاً فقط همین دو فایل `window.X = X` را صریح می‌نویسند.

### اثبات تجربی

`tests/harness_globals.js` هر ۹ فایل را در یک realm جدا اجرا می‌کند:

| | API | Views | Modals | TGBridge | Dossier | App | PullRefresh | AvatarRenderer |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| **قبل** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| **بعد** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

### چرا در چت معمولی مشکلی نبود

آنجا اصلاً جاوااسکریپتی در کار نیست. `handlers/admin.py` مستقیم
`access.is_admin(user_id)` → `user_id in settings.ADMIN_IDS` را چک می‌کند.

### فیکس

یک خط در انتهای هر ماژول:

```js
window.API = API;   // و مشابه برای Views / Modals / TGBridge / Dossier / App
```

در `app.js` این خط **قبل از** `App.init()` قرار گرفته، چون `TGBridge.init()` به
`window.App` نگاه می‌کند.

---

## ۲. آسیب‌پذیری‌های امنیتی (بحرانی — قبل از هر چیز دیگری دیپلوی کنید)

این‌ها با اکسپلویت واقعی روی کد فعلی شما اجرا و تأیید شدند
(`tests/exploit_demo.py`). **هیچ اعتبارنامه‌ای لازم نیست** — فقط یک هدر HTTP که هر
کسی می‌تواند بنویسد.

### ۲.۱ ارتقای کامل دسترسی ادمین (شدیدترین)

```bash
curl -H "X-Telegram-User-Id: 2088114041" https://<دامنه>/site/staff
```

خروجی روی کد فعلی:

```
GET  /site/staff                  → 200   🔓 نشت لیست کامل ادمین‌ها
POST /site/staff (ادمین جدید)     → 200   🔓 مهاجم خودش را ادمین کرد
DEL  /site/signals/1              → 200   🔓 سیگنال حذف شد
POST /site/content/articles       → 200   🔓 محتوا منتشر شد
```

**علت:** `_require_admin` در `routes.py` بعد از چک اول، دوباره *فقط بر اساس
`telegram_id`* دسترسی می‌دهد:

```python
if telegram_id and (int(telegram_id) in ADMIN_IDS or int(telegram_id) in auth.get_admin_ids()):
    session["is_admin"] = True
    return session
```

و روی مسیر تأییدنشده، `telegram_id` مستقیماً از هدر `X-Telegram-User-Id` می‌آید.
این بلوک **بعد از** `_resolve_user_session` اجرا می‌شود، پس تمام محافظت‌های داخل آن
را دور می‌زند — از جمله همان کامنت «فیکس امنیتی بحرانی» که بالایش نوشته شده.

آیدی عددی تلگرام محرمانه نیست؛ از هر پیام فوروارد‌شده‌ای قابل‌کشف است.

**فیکس:** `_require_verified()` — هیچ مسیری به دسترسی ادمین ختم نمی‌شود مگر هویت با
یکی از این دو اثبات شده باشد: توکن سشن معتبر، یا `initData` امضاشده‌ی تلگرام.

### ۲.۲ ارتقا از طریق توکن (مسیر دوم به همان مقصد)

مسیر تأییدنشده `is_admin=False` برمی‌گرداند ولی **یک توکن سشن واقعی صادر می‌کرد**.
دفعه‌ی بعد که همان توکن با `Authorization: Bearer` برگردد،
`auth.get_session()` دوباره از صفر `is_admin` را از روی `telegram_id` حساب می‌کند:

```
توکن صادرشده از همان مسیر، دوباره فرستاده شد → is_admin=True  🔓 ارتقای کامل
```

**فیکس:** در مسیر تأییدنشده اصلاً توکنی صادر نمی‌شود. هویت اثبات‌نشده هرگز به یک
اعتبارنامه‌ی ماندگار تبدیل نمی‌شود.

### ۲.۳ نشت دسترسی از طریق `quota`

```
/site/session → is_admin=False   ولی   quota.is_admin=True
                                        quota.display_role='👑 Super Admin'
```

`app.js` در `updateUserInterface` دقیقاً این را هم چک می‌کند:

```js
const isAdmin = Boolean(isSuper || s?.is_admin === true || quota?.is_admin === true || ...);
```

⇒ پنل ادمین برای هویت اثبات‌نشده باز می‌شد.

**فیکس:** پارامتر `elevate=False` در `get_user_role_and_quota` — دیکشنری quota از پایه
بدون ارتقا ساخته می‌شود.

### ۲.۴ ادعای جعلی مالکیت سیگنال

`_require_owner_or_admin` مالکیت را با مقایسه‌ی `telegram_id` سشن می‌سنجد. روی مسیر
تأییدنشده آن آیدی از هدر می‌آید ⇒ هر کسی می‌توانست ادعا کند صاحب هر سیگنالی است و
نتیجه‌اش را به `win` تغییر دهد. مستقیماً روی لیدربورد و اعتبار کالرهای واقعی اثر دارد.

### ۲.۵ آپلود فایل بدون هیچ احراز هویتی

```python
session = _resolve_user_session(request)   # ← نتیجه هیچ‌وقت چک نمی‌شد
```

نتیجه: `WebUploadOK: uid=anon` — هر غریبه‌ای می‌توانست فایل ۱۰ مگابایتی دلخواه در
`/static/uploads` بنویسد (پسوند `.jpg` می‌شود ولی محتوا اعتبارسنجی نمی‌شود). هم پر شدن
دیسک، هم میزبانی فایل دلخواه روی دامنه‌ی خودتان.

### ۲.۶ تزریق لاگ

`/site/client-log` باز، بدون سقف طول، بدون محدودیت نرخ، و بدون حذف `\n`:

```
# لاگ تولیدشده روی کد فعلی — خط کاملاً مستقل، غیرقابل‌تشخیص از خروجی واقعی سرور:
09-20 20:09:51 [I] AuthOK: uid=999000111 adm=True super=True
```

به‌علاوه: پیام ۲۰هزار کاراکتری بدون برش نوشته می‌شد، و ۲۰۰ خط flood همه ثبت می‌شدند —
یعنی یک حلقه‌ی ساده می‌توانست با rotation، شواهد واقعی را از بین ببرد.

بعد از فیکس:
```
09-20 17:16:49 [I] [JS:?] boot ok 09-20 20:09:51 [I] AuthOK: uid=999000111 ...
                        └─ یک خط، علامت‌خورده به‌عنوان محتوای کلاینت
```

---

## ۳. زیرساخت لاگ

### ۳.۱ دو سیستم لاگ موازی

| | مقصد | چه کسی می‌نوشت |
|---|---|---|
| `settings.setup_logging()` | `./bot.log` | `logging.info(...)` — ipn_server، site_sync، traders، connection |
| `logger.py` | `logs/bot.log` | `logger.info(...)` — routes، auth، ratings |

و `/logs` **فقط دومی** را می‌خواند.

اندازه‌گیری واقعی روی همان اکسپلویت:

```
logs/bot.log  : 27359 بایت   (چیزی که /logs نشان می‌دهد)
./bot.log     : 64066 بایت   (نامرئی برای /logs)
```

**۷۰٪ حجم لاگ اصلاً دیده نمی‌شد.** به‌علاوه چون `propagate` روی logger «memeland» روشن
بود، خطوطش در هر دو فایل و دوبار در کنسول می‌آمدند.

**فیکس:** هندلرها فقط روی root نصب می‌شوند؛ logger نام‌دار هیچ هندلری ندارد و propagate
می‌کند. بعد از فیکس: `./bot.log = 0 بایت`، همه‌چیز یک‌جا، بدون تکرار.

### ۳.۲ لاگ دروغگو

```js
if (window.API && typeof API.getSignals === 'function') { ... }   // ساکت رد می‌شد
// ...
window.logEventThrottled('APP', 'loadSignalsOK', { total: 0, open: 0 });
```

یعنی لاگ می‌گفت «موفق، صفر سیگنال» در حالی که **اصلاً درخواستی زده نشده بود**. همین یک
خط، چند دور دیباگ را به بیراهه برده. حالا:

```
[APP] loadSignalsSkipped reason=API missing        ← سطح ERROR
```

### ۳.۳ لاگر جدید کلاینت (`static/js/logger.js`)

| قبل | بعد |
|---|---|
| هر خط = یک `fetch` جدا (۱۴ درخواست در ۴۰ ثانیه) | batch: هر ۲.۵ ثانیه / ۸ رویداد / فوری برای خطا |
| همه‌چیز INFO، حتی خطای JS | سطوح `E/W/I/D` — سرور با levelname درست ثبت می‌کند |
| `sid` و `build` در هر خط تکرار می‌شد | یک‌بار در هر batch ⇒ حجم تقریباً نصف |
| دو تعریف موازی (inline + fallback در `api.js`) | یک فایل، یک تعریف |
| throttle: تعداد سرکوب‌شده گم می‌شد | flush زمان‌دار با `repeated=N` |
| لاگ لحظه‌ی خروج/کرش گم می‌شد | `sendBeacon` روی `pagehide` + `visibilitychange` |
| — | ring buffer محلی: `MHLog.tail(40)` در کنسول |

### ۳.۴ ابزارهای تشخیص جدید

**`/diag` در ربات** — یک عکس فوری، بدون خواندن لاگ:

```
🩺 وضعیت سیستم
هویت شما:  آیدی · در ADMIN_IDS ✅/❌ · در جدول staff ✅/❌
دیتابیس:   مسیر بات · مسیر سایت · تعداد سیگنال فید · سشن‌های فعال
وب‌اپ:     index.html ✅ · تعداد فایل JS · logger.js دیپلوی شده ✅ · SITE_URL
لاگ:       حجم · تعداد خطا · تعداد هشدار
```

**`/logs` با فیلتر:**

```
/logs            ۳۰ خط آخر
/logs 80         ۸۰ خط آخر
/logs err        فقط ERROR
/logs warn       WARNING و بالاتر
/logs js         فقط لاگ‌های کلاینت وب‌اپ
/logs auth       هر خطی شامل «auth»
/logs err 60     ۶۰ خط آخر از ERROR ها
/logs file       فایل کامل
```

**`GET /site/diag`** (فقط ادمین) — همان اطلاعات به‌صورت JSON، از داخل وب‌اپ.

### ۳.۵ کاهش نویز

`httpx` در سطح INFO برای هر polling تلگرام یک خط می‌نویسد. این‌ها (به‌همراه
`telegram`, `apscheduler`, `aiohttp.access`, `asyncio`, `urllib3`) به WARNING منتقل شدند.

---

## ۴. سایر باگ‌های یافت‌شده

### ۴.۱ نشت جدول `sessions`

`_resolve_user_session` به ازای **هر درخواست** یک ردیف سشن جدید می‌ساخت. اندازه‌گیری:

```
۲۰ درخواست ساده‌ی فید → ردیف sessions: 5 ⟶ 25
```

هر لود صفحه ≥۲ درخواست می‌زند، pull-refresh هم همین‌طور، و هیچ‌وقت چیزی پاک نمی‌شد.
هر توکن ۳۰ روز اعتبار دارد.

**فیکس:** استفاده‌ی مجدد از توکن زنده (`find_active_token`) + جاب پاک‌سازی روزانه
(`purge_expired_sessions`) + حذف کامل صدور توکن در مسیر تأییدنشده.
بعد از فیکس: `0 ⟶ 0`.

### ۴.۲ واگرایی دو دیتابیس

ربات کادر را در `staff` دیتابیس **خودش** (`signals.db`) می‌نویسد؛ سایت `staff` دیتابیس
**سایت** (`site.db`) را می‌خواند. یعنی هر VIP Helper که از داخل ربات اضافه شود، در
وب‌اپ ادمین نیست. فقط `ADMIN_IDS` (از `.env`) اتفاقی در هر دو کار می‌کرد.

**فیکس:** `_is_staff_admin` هر دو دیتابیس را چک می‌کند.

### ۴.۳ رول کاربر همیشه `rookie`

`get_user_role_and_quota` جدول `users` را در `site.db` می‌جست — جدولی که آنجا **وجود
ندارد** (فقط در `signals.db` است). چون کد اول با `sqlite_master` وجود جدول را چک
می‌کرد، هیچ خطایی نمی‌داد و **بی‌صدا** همیشه `rookie` برمی‌گرداند.

اثر عملی: سقف سهمیه‌ی روزانه‌ی همه‌ی کاربران روی ۳ گیر کرده بود، صرف‌نظر از رول
واقعی‌شان (`explorer`=۵، `guardian`=۸، `alpha`=۱۵، `og`=۵۰).

> `traders.py` دقیقاً همین باگ را قبلاً برای `username` فیکس کرده بود، ولی اینجا جا
> مانده بود.

### ۴.۴ پنجره‌ی زمانی لیدربورد

`created_at` همه‌جا با `datetime.utcnow()` نوشته می‌شود، ولی `handle_leaderboard` از
`datetime.now()` (ساعت محلی سرور) استفاده می‌کرد. روی سرور با ساعت ایران، پنجره‌ی
«هفته» ۳:۳۰ ساعت جابه‌جا می‌شد و سیگنال‌های مرزی از لیدربورد می‌افتادند.

### ۴.۵ خطای ۵۰۰ به‌جای ۴۰۰

`int(request.match_info["id"])` بدون `try` در سه هندلر. یک درخواست به
`/site/signals/abc` باعث `ValueError` مدیریت‌نشده، پاسخ ۵۰۰ و یک traceback در لاگ می‌شد.

### ۴.۶ ویرایش سیگنال شکننده

کل بدنه به `edit_signal(**body)` پاس داده می‌شد. هر کلید اضافه‌ای که فرانت‌اند بفرستد
(مثل `id` یا `token`) باعث `ValueError` و پاسخ ۴۰۰ برای یک ویرایش کاملاً معتبر می‌شد.

### ۴.۷ SQLite بدون WAL

`site.db` با `timeout` پیش‌فرض (۵ ثانیه) و `journal_mode=DELETE`. وب‌سرور و ربات در یک
پروسه و یک event loop اجرا می‌شوند و همه‌ی فراخوانی‌های sqlite3 همگام‌اند.

> نشانه‌ی این‌که واقعاً رخ می‌داده: `site_sync.py` یک retry اختصاصی برای
> `"database is locked"` دارد.

**فیکس:** `WAL` + `synchronous=NORMAL` + `busy_timeout=8000` + `timeout=8.0`.

---

## ۵. اعتبارسنجی

### ۵.۱ تست یکپارچه — ۴۷ مورد، همه موفق

```
۱  احراز هویت با initData معتبر (HMAC واقعی)      ۴ مورد ✅
۲  استفاده‌ی مجدد از سشن                          ۱ مورد ✅
۳  مسیر تأییدنشده                                 ۵ مورد ✅
۴  رد کردن initData جعلی                          ۱ مورد ✅
۵  ارتقای دسترسی (۵ مسیر مختلف)                   ۸ مورد ✅
۶  فید سیگنال + قفل VIP                           ۲ مورد ✅
۷  لاگ کلاینت (batch/سطح/تزریق/نرخ)               ۵ مورد ✅
۸  ورودی‌های بد ⇒ ۴۰۰ نه ۵۰۰                       ۳ مورد ✅
۹  آپلود بدون احراز هویت                          ۱ مورد ✅
۱۰ /site/diag                                     ۴ مورد ✅
۱۱ سیستم لاگ یکپارچه                              ۷ مورد ✅
۱۲ ادعای جعلی مالکیت                              ۲ مورد ✅
۱۳ خواندن عمومی هنوز باز است                      ۳ مورد ✅
```

اجرا: `cd signal_bot && python3 tests/test_integration.py`

> دو مورد از این تست‌ها در اولین اجرا **شکست خوردند** و باگ‌های ۲.۱ و ۲.۴ را پیدا کردند
> — باگ‌هایی که با خواندن کد پیدا نکرده بودم. تست‌ها را نگه دارید.

### ۵.۲ نمایش اکسپلویت

`tests/exploit_demo.py` روی هر دو نسخه اجرا می‌شود و تفاوت را نشان می‌دهد.

### ۵.۳ harness گلوباله‌ها

`tests/harness_globals.js` — اثبات مستقل باگ ریشه‌ای، بدون نیاز به مرورگر.

---

## ۶. فایل‌های تغییریافته

| فایل | خطوط تغییر |
|---|---:|
| `memeland_site/static/js/logger.js` | **جدید** — ۲۱۵ |
| `memeland_site/index.html` | ۱۳۱ |
| `memeland_site/static/js/app.js` | ۷۱ |
| `memeland_site/static/js/api.js` | ۵۳ |
| `memeland_site/static/js/{views,modals,telegram,dossier}.js` | ۲۲ هرکدام |
| `signal_bot/signal_bot/site/routes.py` | ۳۲۸ |
| `signal_bot/signal_bot/logger.py` | ۱۸۴ |
| `signal_bot/signal_bot/site/auth.py` | ۱۸۲ |
| `signal_bot/main.py` | ۱۳۶ |
| `signal_bot/signal_bot/site/db.py` | ۲۷ |
| `signal_bot/signal_bot/config/settings.py` | ۲۰ |

هیچ تغییری در schema دیتابیس لازم نیست (فقط یک ایندکس `IF NOT EXISTS` اضافه شده).

---

## ۷. دیپلوی

```bash
# ۱. بکاپ
cp -r MEMELAND-main MEMELAND-backup-$(date +%F)

# ۲. اعمال پچ
cp -r memeland-patch/memeland_site/*  MEMELAND-main/memeland_site/
cp -r memeland-patch/signal_bot/*     MEMELAND-main/signal_bot/

# ۳. ری‌استارت
# (ری‌استارت معمول خودتان)
```

### تأیید بعد از دیپلوی — به همین ترتیب

**گام ۱ — در ربات:**
```
/diag
```
هر چهار مورد باید ✅ باشند. اگر «logger.js دیپلوی شده ❌» بود، فایل‌های استاتیک
به‌درستی کپی نشده‌اند.

**گام ۲ — وب‌اپ را باز کنید، بعد:**
```
/logs 40
```
باید ببینید:
```
[BOOT] index.html build=2026-09-20.1
[BOOT] logger.js build=2026-09-20.1
[BOOT] modules build=2026-09-20.1 API=1 Views=1 Modals=1 TGBridge=1 Dossier=1
       PullRefresh=1 AvatarRenderer=1 tgSdk=1 initLen=<عددی بزرگ‌تر از صفر>
[AUTH] attempt hasTg=true initLen=... tgUid=2088114041
WebAppAuthReq: path=/site/auth initDataLen=... 
AuthOK: uid=2088114041 adm=True super=True
[APP] ready sessUid=2088114041 tgUid=2088114041 adm=true verified=1 role=admin sigCount=N
```

**نکته‌ی کلیدی:** خط `[BOOT] modules` جایگزین همه‌ی `guardFail`های پراکنده شده. اگر
هر کدام `=0` بود، آن فایل لود نشده (مشکل دیپلوی یا کش) — نه باگ منطقی.

**گام ۳ — اگر هنوز `adm=false` بود**, دو حالت ممکن است:

| نشانه در لاگ | معنی | راه‌حل |
|---|---|---|
| `verified=0` | `initData` خالی رسیده — سرور عمداً دسترسی نمی‌دهد | مقدار `initLen` را ببینید؛ اگر ۰ است، مشکل از سمت تلگرام/وب‌ویو است نه کد |
| `verified=1` ولی `adm=false` | هویت اثبات شده ولی آیدی در لیست ادمین نیست | `/diag` → «در ADMIN_IDS» را چک کنید |
| `AuthFail: TMA init_data invalid` | `BOT_TOKEN` سرور با توکن ربات میزبان وب‌اپ یکی نیست | `.env` را چک کنید |

---

## ۸. تغییرات رفتاری که باید بدانید

1. **ثبت سیگنال، آپلود تصویر و همه‌ی عملیات ادمین حالا `initData` امضاشده لازم دارند.**
   داخل وب‌اپ تلگرام همیشه موجود است. ولی اگر صفحه را مستقیم در مرورگر باز کنید،
   دیگر نمی‌توانید بنویسید. این مصالحه عمدی است — بدون آن، هدر جعلی کافی بود.
   خواندن (فید، لیدربورد، مقالات) همچنان عمومی است.

2. **`?v=8.0.0`** روی تمام فایل‌های استاتیک برای ابطال کش وب‌ویو تلگرام.

3. **`window.__MH_BUILD`** هنوز دستی است. پیشنهاد: از CI یا `git rev-parse --short HEAD`
   پرش کنید تا دیگر یادتان نرود.

---

## ۹. آنچه بررسی نشده

برای صداقت گزارش — این‌ها را **بررسی نکرده‌ام**:

- `handlers/` (۲۳۰۰ خط: admin، signals، payments، support) — فقط برای فهمیدن منطق
  تشخیص ادمین خوانده شدند، نه ممیزی کامل.
- `services/` جز `webapp_auth` و `site_sync` — مخصوصاً `price_feed`,
  `alpha_score`, `payments`, `watermark`.
- `web/ipn_server.py` — فقط ثبت مسیرها خوانده شد. مسیر IPN
  (وب‌هوک پرداخت) ممیزی نشده و پول در جریان است؛ ارزش یک بررسی جدا را دارد.
- CSS — اصلاً باز نشده.
- عملکرد واقعی روی دستگاه/وب‌ویوی تلگرام — همه‌ی تست‌ها headless بودند.

پیشنهاد بعدی، به ترتیب اولویت: مسیر IPN، سپس `handlers/payments.py`.

---
---

# دور دوم — سینک دوطرفه، فهرست کادر، فیلتر نمایش

نسخه‌ی بیلد: `2026-09-20.2` · استاتیک: `?v=8.1.0`

## ۱۰. سیگنال‌ها بین ربات و وب‌اپ سینک نبودند

**علت:** سینک از ابتدا **یک‌طرفه** بود. `site_sync.push_signal_created` سیگنال
تأییدشده‌ی ربات را به سایت می‌فرستاد. جهت معکوس **اصلاً وجود نداشت** — و چون
همه‌ی نماهای ربات از جدول `signals` دیتابیس ربات می‌خوانند، سیگنالِ ثبت‌شده در
وب‌اپ در چت اصلاً وجود نداشت. برعکسش هم برای سیگنال‌های قدیمی صادق بود، چون
`push_signal_created` فقط روی رویداد «تأیید جدید» صدا زده می‌شد و هیچ backfill ای
نبود.

**فیکس:**

- ستون `site_signal_id` در `signals` دیتابیس ربات (لینک معکوس؛ `bot_signal_id`
  از قبل در سمت سایت بود)
- `site_sync.push_signal_from_site()` — وب‌اپ ← ربات، با `status='approved'`
- `site_sync.push_result_from_site()` — نتیجه از وب‌اپ به همان
  `results.apply_result` که مسیر ربات استفاده می‌کند، تا امتیاز/استریک/سطح
  دقیقاً یکسان حساب شوند
- `site_sync.delete_from_site()` — حذف در وب‌اپ، حذف در ربات
- `site_sync.reconcile()` — backfill دوطرفه برای هرچه قبلاً ثبت شده
- دستور `/sync_all` (گزارش) و `/sync_all apply` (اعمال)

**نکته‌ی ظریفی که بدون آن فیکس ناقص می‌ماند:** تقریباً همه‌ی کوئری‌های ربات
`JOIN users u ON s.user_id=u.user_id` دارند. کاربری که فقط از وب‌اپ استفاده کرده
و هیچ‌وقت `/start` نزده، ردیف `users` ندارد — سیگنالش با JOIN حذف می‌شود و **باز
هم نامرئی می‌ماند**. پس `push_signal_from_site` اول وجود ردیف `users` را تضمین
می‌کند.

**نگاشت نتیجه:** دو طرف مدل متفاوتی دارند — سایت متن آزاد + `open/win/loss`، ربات
کلید بسته‌ی متصل به جدول امتیاز. نگاشت بر اساس ضریب ایکس (۲ایکس = ‎+۱۰۰٪‎،
۵ایکس = ‎+۴۰۰٪‎، ۱۰ایکس = ‎+۹۰۰٪‎)، و در نبود عدد، محافظه‌کارانه‌ترین حالت برد
(`win_sl`، ۲ امتیاز) — نه بیشترین.

## ۱۱. بخش مدیریت کادر کاملاً خالی بود

سه علت هم‌زمان:

1. **سوپرادمین‌ها در هیچ جدولی ردیف ندارند.** `ADMIN_IDS` از `.env` می‌آید. برای
   پروژه‌ای که تنها ادمینش از `.env` است، یعنی لیست همیشه خالی.
2. **کادرِ اضافه‌شده از داخل ربات خوانده نمی‌شد** — `staff_repo` در `signals.db`
   می‌نویسد، `get_staff_list` فقط `site.db` را می‌خواند.
3. **نقش‌های تریدر اصلاً در جدول `staff` نیستند** — در `users.role` دیتابیس
   *ربات*‌اند. پس تب «تریدرهای ویژه» ذاتاً نمی‌توانست چیزی نشان دهد.

و یک باگ نوشتن: `add_or_update_staff` همیشه `UPDATE users SET role=?` روی
`site.db` می‌زد، جایی که جدول `users` **وجود ندارد**. پس اعطای نقش تریدر
بی‌صدا هیچ کاری نمی‌کرد.

**فیکس:** `get_staff_list` هر چهار منبع را ادغام می‌کند (ADMIN_IDS، `staff` هر دو
دیتابیس، `users.role` ربات)، نام‌ها از `users` ربات خوانده می‌شوند (نه فقط
`sessions` که تنها کسانی را دارد که وب‌اپ را باز کرده‌اند)، و
`add_or_update_staff` بر اساس نوع نقش مسیردهی می‌کند: کادری → `staff` هر دو
دیتابیس، تریدر → `users.role` ربات با حذف ردیف `staff` (تنزل درجه).

## ۱۲. از ۸ سیگنال فقط ۲ تا نمایش داده می‌شد

در لاگ خودتان بود ولی گزارش نکرده بودید:

```
[VIEW] render tab=active total=8 filtered=2
```

۶ سیگنال جایی حذف می‌شدند و هیچ راهی نبود بفهمیم کدام فیلتر مقصر است. فیلترها در
`localStorage` ذخیره می‌شوند و بین باز شدن‌های اپ باقی می‌مانند — اگر یک‌بار
«امروز» انتخاب شده باشد، هفته‌ی بعد سیگنال‌ها ناپدید می‌شوند و پیام «سیگنال فعالی
موجود نیست» گمراه‌کننده است.

**فیکس:**

- شمارنده‌ی جدا برای هر شرط:
  `[VIEW] render tab=active total=8 kept=2 dStatus=0 dSearch=0 dTime=6 dChannel=0 dRisk=0 filters=newest/today/all/all`
  — سطح `W` وقتی چیزی به‌خاطر فیلتر پنهان شده
- نوار هشدار در UI: «🔎 ۶ سیگنال با فیلترهای فعلی پنهان شده» + دکمه‌ی پاک‌کردن

## ۱۳. باگ منطقه‌ی زمانی در خروجی API

`created_at` با `datetime.utcnow().isoformat()` ذخیره می‌شود — رشته‌ای بدون
نشانگر منطقه‌ی زمانی. طبق استاندارد ECMAScript، `new Date()` روی چنین رشته‌ای آن
را **ساعت محلی** تفسیر می‌کند، نه UTC. روی دستگاه ایرانی (‎+۳:۳۰‎) هر سیگنال
۳:۳۰ ساعت قدیمی‌تر دیده می‌شد و فیلتر «امروز/این هفته» را در لبه‌ها اشتباه می‌کرد.

فیکس در خروجی API (`Z` اضافه می‌شود)، نه در دیتابیس — تا مقایسه‌های رشته‌ای موجود
نشکنند.

## ۱۴. اعتبارسنجی دور دوم

`tests/test_sync_staff.py` — ۴۳ مورد، همه موفق:

```
۱  سیگنال وب‌اپ → دیتابیس ربات            ۶ مورد ✅
۲  نتیجه از وب‌اپ → امتیاز در ربات          ۳ مورد ✅
۳  نگاشت متن نتیجه → کلید امتیاز            ۸ مورد ✅
۴  سیگنال قدیمیِ ربات → backfill به سایت    ۴ مورد ✅
۵  سیگنال قدیمیِ سایت → backfill به ربات    ۴ مورد ✅
۶  حذف از وب‌اپ → حذف از ربات               ۲ مورد ✅
۷  فهرست کادر                               ۵ مورد ✅
۸  اعطای نقش از وب‌اپ                        ۷ مورد ✅
۹  سلب دسترسی                                ۳ مورد ✅
۱۰ created_at با نشانگر UTC                  ۱ مورد ✅
```

به‌علاوه `tests/test_integration.py` (۴۷ مورد) دوباره اجرا شد — بدون رگرسیون.
**مجموع: ۹۰ تست، همه موفق.**

## ۱۵. دیپلوی دور دوم

```bash
./APPLY.sh /path/to/MEMELAND-main
# ربات را ری‌استارت کنید
```

سپس، **به همین ترتیب**:

```
/diag              ← بخش جدید «سینک ربات ↔ سایت» را ببینید
/sync_all          ← فقط گزارش می‌دهد، چیزی نمی‌نویسد
/sync_all apply    ← اگر گزارش درست بود، اعمال کنید
/diag              ← باید «✅ همگام» شود
```

ستون `site_signal_id` خودکار با `ALTER TABLE` در `init_db()` اضافه می‌شود؛ کار
دستی روی دیتابیس لازم نیست.

⚠️ `/sync_all apply` **تصویر** سیگنال‌های قدیمی را منتقل نمی‌کند (فقط متن و
متادیتا). آپلود دوباره‌ی صدها فایل از تلگرام کند است و ریسک rate-limit دارد.
سیگنال‌های جدید تصویرشان از مسیر عادی منتقل می‌شود.

---
---

# دور سوم — عدم حذف سیگنال‌ها، ممیزی نتایج/سوابق و لیدربورد

نسخه‌ی بیلد: `2026-09-21.1` · استاتیک: `?v=8.2.0`

## ۱۶. سیگنال‌ها حذف نمی‌شدند — یک محدودیت شناخته‌شده‌ی Telegram Mini App

**تشخیص:** در ۵۰ ثانیه لاگ بسیار پرتراکم (چندین fetch در ثانیه، یعنی ادمین
واقعاً داشت با اپ کار می‌کرد)، **حتی یک خط `SigDel:` هم دیده نشد.** یعنی
درخواست DELETE اصلاً به سرور نمی‌رسید — مشکل سمت کلاینت بود، نه سرور.

**علت ریشه‌ای:** دکمه‌ی حذف قبل از هر کاری این را اجرا می‌کرد:

```js
if (window.TGBridge) {
  const conf = await TGBridge.showConfirm('...');
  if (!conf) return;
}
```

این با یک محدودیت مستند اکوسیستم Telegram Mini App برخورد می‌کند: **Mini App
ها داخل Telegram Web/Desktop در یک `<iframe>` sandbox‌شده لود می‌شوند که
popup های بومی مرورگر (`alert`/`confirm`/`prompt`) در آن مسدودند** — دقیقاً
همان محدودیتی که وجود `WebApp.showAlert`/`showConfirm` را در SDK تلگرام
توجیه می‌کند. اگر به هر دلیلی (نسخه‌ی قدیمی‌تر Bot API، یا یک کلاینت خاص)
`TGBridge.showConfirm` به‌جای متد رسمی به `window.confirm()` خام فال‌بک کند،
آن فراخوانی داخل یک `new Promise` بدون `try/catch` صدا زده می‌شود — اگر
`confirm()` در آن iframe مسدود باشد یا throw کند، **Promise برای همیشه
معلق می‌ماند** و خط بعدی (خودِ حذف) هرگز اجرا نمی‌شود. نه خطایی، نه لاگی —
دقیقاً همان چیزی که شما توصیف کردید.

**فیکس:** یک مودال تأیید کاملاً خودمان (`Modals.confirmAction`)، بازساخته
از همان `modalOverlay` موجود — بدون هیچ وابستگی به API تلگرام یا popup
بومی مرورگر. این با هر کلاینتی (موبایل/وب/دسکتاپ) یکسان کار می‌کند چون فقط
HTML/CSS/JS ساده است، نه یک API که رفتارش به نسخه‌ی کلاینت وابسته باشد.
جایگزین شد در هر سه محل: حذف سیگنال، حذف مقاله، سلب دسترسی کادر.

Promise هرگز معلق نمی‌ماند — حتی اگر کاربر با دکمه‌ی ✕ ببندد (نه با
دکمه‌های تأیید/انصراف)، `closeModal()` آن را `false` می‌کند. این با یک
harness مستقل (بدون DOM واقعی) در ۵ سناریو تأیید شد (کلیک تأیید، کلیک
انصراف، بستن با ✕، بدون هیچ وابستگی به `showConfirm`، و دو `confirm`
هم‌زمان که اولی را آزاد می‌کند) — همه موفق.

لاگ تشخیصی هم اضافه شد تا دفعه‌ی بعد قطعی معلوم شود کجای مسیر گیر کرده:

```
[JS] [MODALS] deleteConfirmShown sid=8
[JS] [MODALS] deleteConfirmResult sid=8 confirmed=true
[JS] [MODALS] deleteHttp sid=8 status=200 ok=true
```

سمت سرور هم: `SigDel: sid=8 ok=true botDel=ok by=2088114041` — حالا نتیجه
(`ok`)، وضعیت سینک با ربات، و اینکه چه کسی حذف کرده، هر سه در یک خط است. و
اگر درخواست اصلاً به سرور نرسد (دقیقاً سناریوی این باگ)، غیبت کامل
`SigDel:` در کنار وجود `SigDelDenied` یا نبود هر دو، بلافاصله می‌گوید مشکل
کجاست.

## ۱۷. اشتباهی که در پچ قبلی مرتکب شدم — ناسازگاری منطقه‌ی زمانی بین دو دیتابیس

در دور دوم، `handle_leaderboard` را طوری فیکس کردم که از `datetime.now(utc)`
برای محاسبه‌ی بازه‌ی «هفته» استفاده کند، با این توجیه که «created_at همه‌جا
UTC است». **این فرض اشتباه بود.** درست است برای `site.db`، ولی
`db/signals_repo.py::insert_signal` (دیتابیس *ربات*) از ابتدا با
`datetime.now()` — **ساعت محلی سرور** — می‌نوشت، و لیدربورد signal_givers
دقیقاً از همین جدول می‌خواند.

پیش از پچ قبلی، این خودش مشکلی نبود (چون هم نوشتن و هم خواندن محلی بودند،
داخلی سازگار). اما پچ من یک طرف مقایسه را UTC کرد بدون اینکه طرف دیگر را
چک کنم — یعنی برای سیگنال‌های بومیِ ربات (نه سینک‌شده از سایت)، یک ناسازگاری
~۳.۵ ساعته *ایجاد* کردم که قبلاً وجود نداشت. به‌علاوه، سینک دوطرفه‌ای که در
همان دور اضافه شد، رکوردهای UTC (از سایت) را در همان جدولی می‌نوشت که
رکوردهای بومی محلی بودند — یعنی یک ستون `created_at` با **دو قرارداد
متفاوت همزمان**، که با مقایسه‌ی رشته‌ای ساده‌ی SQLite اصلاً قابل مقایسه‌ی
درست نیستند.

**فیکس ریشه‌ای (نه فقط برگرداندن پچ قبلی):** جدول `signals` دیتابیس ربات
از این پس **سراسر UTC** است — هم نوشتن، هم خواندن:

- `insert_signal` (سیگنال بومی ربات) → `datetime.utcnow()`
- `insert_from_site` (سینک از سایت) → fallback به `datetime.utcnow()` هم
  هماهنگ شد
- `daily_signal_count` → مرز «امروز» حالا UTC است (⚠️ **تغییر رفتاری**: سقف
  سهمیه‌ی روزانه از این پس در نیمه‌شب UTC ریست می‌شود، نه نیمه‌شب محلی سرور)
- `formatters/texts.py::leaderboard_text` (دستور `/leaderboard` **داخل
  چت**) هم به همین دلیل UTC شد — وگرنه لیدربورد چت با لیدربورد وب‌اپ برای
  همان بازه‌ی زمانی عدد متفاوت می‌داد، که خودش یک باگ جدید بود.

**دامنه‌ی عمدی این فیکس:** فقط جدول `signals` ربات دست خورد. بقیه‌ی
تاریخ‌های سراسر کد ربات (`users.joined_at`، `prize_repo`، `rewards_repo`،
`staff_repo`، `caller_donations_repo`) همچنان با `datetime.now()` محلی
نوشته و مقایسه می‌شوند — این‌ها **داخلی سازگارند** (چون نه سینک می‌شوند و
نه با چیز دیگری قاطی) و دست‌نخورده ماندند تا ریسک این پچ محدود بماند. اگر
در آینده خواستید کل سیستم یک‌دست UTC شود، این فایل‌ها فهرست دقیق باقی‌مانده
هستند.

یک لاگ تشخیصی فشرده هم اضافه شد تا اگر روزی لیدربورد خالی یا عجیب دیده شد،
یک خط جواب بدهد:

```
LeaderboardDeliver: period=week since=2026-09-13T18:03:08 givers=2 callers=0
```

## ۱۸. خودترمیمی سینک برای نتیجه‌ی سیگنال‌های قدیمی

`push_result_from_site` قبلاً اگر لینک ربات را پیدا نمی‌کرد (سیگنال‌های
سایت-محور قبل از دیپلوی سینک دوطرفه)، فقط لاگ می‌کرد و متوقف می‌شد — یعنی
تا وقتی ادمین یادش می‌افتاد `/sync_all` بزند، نتیجه فقط در سایت ثبت می‌شد و
امتیاز/لیدربورد ربات هیچ‌وقت به‌روز نمی‌شد. این دقیقاً همان چیزی بود که در
لاگ دیدید:

```
site_sync: سیگنال سایت #8 معادل رباتی نداره (سیگنال قدیمی؟)
SigResult: sid=8 res=+۱ stat=open botSync=skip
```

**فیکس:** حالا قبل از تسلیم شدن، خودش تلاش می‌کند لینک را همان لحظه بسازد
(دقیقاً همان کاری که `/sync_all` می‌کرد، ولی خودکار و آنی)، سپس نتیجه را
ثبت می‌کند. دیگر وابسته به این نیست که ادمین یادش بماند دستور را بزند.
پاسخ اندپوینت هم حالا `bot_synced` را برمی‌گرداند (قبلاً فقط در لاگ سرور
بود) تا فرانت‌اند/تست بدون خواندن لاگ بفهمد سینک واقعاً انجام شده یا نه.

آپدیت مکرر نتیجه‌ی همان سیگنال هم تست شد (مثلاً از ‎+۴۰۰٪‎ به ‎+۹۵۰٪‎) — امتیاز
به‌درستی فقط دلتا اضافه می‌کند، نه از صفر جمع نمی‌شود.

## ۱۹. سوابق تغییر نتیجه (history) در API بود ولی هیچ‌جا نمایش داده نمی‌شد

فیلد `history` (لیست نتایج قبلی هر سیگنال) از قبل در پاسخ API وجود داشت،
اما هیچ کد فرانت‌اندی آن را نمی‌خواند — «بخش نتایج و سوابق» فقط لیست
سیگنال‌های بسته‌شده را نشان می‌داد، بدون سیر تغییر نتیجه. حالا در کارت
جزئیات هر سیگنال، اگر سوابقی باشد، به‌صورت «📜 سوابق تغییر نتیجه» (جدیدترین
اول) نمایش داده می‌شود. به‌علاوه یک باگ کوچک‌تر همراه آن پیدا و فیکس شد:
`changed_at` هم مثل `created_at` بدون نشانگر UTC ذخیره می‌شد و دچار همان
مشکل تفسیر اشتباه به‌عنوان ساعت محلی در مرورگر بود.

## ۲۰. پیرو امنیتی: توکن‌های صادرشده قبل از پچ اول

یک نکته‌ی مهم که باید بدانید: پچ دور اول، **مسیر آینده** را بست (دیگر از
مسیر ناامن توکن صادر نمی‌شود)، اما هر توکنی که **قبل از آن دیپلوی** از همان
مسیر آسیب‌پذیر صادر شده بود، همچنان تا ۳۰ روز در جدول `sessions` معتبر
می‌ماند — و `get_session()` فرقی بین «توکنِ صادرشده از مسیر امن» و «توکنِ
صادرشده قبل از فیکس» نمی‌گذارد.

دستور جدید `/wipe_sessions confirm` همه‌ی نشست‌ها را یک‌جا باطل می‌کند (همه
باید دوباره وارد وب‌اپ شوند — این خودکار و بدون دردسر است، چون ورود مجدد
از همان `initData` امضاشده استفاده می‌کند). **توصیه: یک‌بار، همین الان
اجرا کنید** — چون نمی‌دانیم قبل از پچ اول، کسی از آن آسیب‌پذیری استفاده
کرده بوده یا نه.

## ۲۱. اعتبارسنجی دور سوم

`tests/test_round3_fixes.py` — ۲۶ مورد، همه موفق:

```
۱  خودترمیمی نتیجه برای سیگنال لینک‌نشده        ۵ مورد ✅
۲  آپدیت دوباره‌ی نتیجه (دلتای امتیاز درست)      ۳ مورد ✅
۳  هماهنگی UTC سایت↔ربات (< ۵ ثانیه اختلاف)      ۱ مورد ✅
۴  لیدربورد سیگنال تازه را در بازه‌ی هفته می‌بیند  ۲ مورد ✅
۵  daily_signal_count بر مبنای UTC               ۱ مورد ✅
۶  لیدربورد چت و وب‌اپ همسو                      ۲ مورد ✅
۷  history با نشانگر UTC                         ۳ مورد ✅
۸  رگرسیون مسیر حذف                              ۶ مورد ✅
۹  wipe_sessions                                 ۳ مورد ✅
```

به‌علاوه یک harness مستقل جاوااسکریپت (بدون DOM واقعی) مکانیزم
`confirmAction` را در ۵ سناریو تأیید کرد. و دو مجموعه‌ی قبلی
(`test_integration.py` ۴۷ مورد، `test_sync_staff.py` ۴۳ مورد) بدون رگرسیون
دوباره اجرا شدند.

**مجموع تجمعی سه دور: ۱۱۶ تست خودکار، همه موفق.**

## ۲۲. دیپلوی دور سوم

```bash
./APPLY.sh /path/to/MEMELAND-main
```

سپس، **این بار به همین ترتیب و فقط یک‌بار**:

```
/wipe_sessions confirm     ← همه باید دوباره وب‌اپ را باز کنند (بدون دردسر)
```

بعد از آن، چرخه‌ی معمول:

```
/diag
/sync_all
/sync_all apply   (در صورت لزوم)
```

⚠️ اگر سیگنال‌های approved-ی دارید که created_at قدیمی (محلی، قبل از این
پچ) دارند، آن‌ها همچنان با قرارداد محلی در دیتابیس می‌مانند — این پچ
داده‌های موجود را migrate نمی‌کند، فقط از این پس چیزی جدید با ناسازگاری
نوشته نمی‌شود. اگر لیدربورد یک روز عدد عجیبی نشان داد، `LeaderboardDeliver`
در لاگ اولین جای چک است.

---
---

# دور چهارم — ممیزی جامع کل کدبیس

نسخه‌ی بیلد وب‌اپ بدون تغییر ماند (`2026-09-21.1` / `?v=8.2.0`) — هیچ فایل JS/HTML این دور تغییر نکرد؛ همه‌ی فیکس‌ها سمت پایتون (ربات) بودند.

این دور، به‌جای پاسخ به یک علامت مشخص، یک ممیزی خط‌به‌خط کل کدبیس بود: مسیر پرداخت/IPN، همه‌ی هندلرهای ربات، همه‌ی سرویس‌ها، و لایه‌های دیتابیس. هدف: پیدا کردن باگ‌های خاموش قبل از اینکه کسی به آن‌ها برخورد کند.

## ۲۳. لینک معکوس سیگنال‌های بومیِ ربات هرگز ساخته نمی‌شد

**مهم‌ترین یافته‌ی این دور.** `push_signal_created` — تابع اصلی سینک ربات→سایت، پراستفاده‌ترین مسیر سینک در کل پروژه (هر سیگنالی که با `/fullsignal`/`/fastcall` ثبت و تأیید می‌شود از همین رد می‌شود) — جهت رفت (`site.signals.bot_signal_id`) را می‌ساخت ولی هیچ‌وقت جهت برگشت (`bot.signals.site_signal_id`) را تنظیم نمی‌کرد.

اثر عملی: توابع دور سوم که برای «حذف» و «خودترمیمی نتیجه» نوشته شدند (`delete_from_site`, `push_result_from_site`) هر دو بر پایه‌ی `signals_repo.get_by_site_id()` کار می‌کنند — که فقط زمانی جواب می‌دهد که لینک معکوس تنظیم شده باشد. برای سیگنال‌های بومیِ ربات (اکثریت قریب‌به‌اتفاق سیگنال‌ها)، این لینک هیچ‌وقت ساخته نمی‌شد. یعنی:

> **حذف یک سیگنالِ بومیِ ربات از وب‌اپ، سمت سایت را پاک می‌کرد ولی سمت ربات دست‌نخورده باقی می‌ماند** — همان «حذف نمی‌شود»، این‌بار برای اکثریت سیگنال‌ها، نه فقط سیگنال‌های وب‌اپ‌محور که در دور سوم فیکس شد.

با یک اسکریپت تکرار مستقل تأیید شد (قبل از فیکس: `None`، بعد از فیکس: id درست)، فیکس شد (یک خط: `signals_repo.link_site_id(bot_signal_id, site_id)` بعد از ساخت ردیف سایت)، و در تست رسمی پوشش داده شد.

## ۲۴. عکس سیگنال‌های تأییدشده در ربات هرگز به سایت نمی‌رسید

`services/image_upload.py` دو تابع آپلود دارد که هر دو به `SITE_SUPABASE_URL`/`SERVICE_KEY` (سرویسی که پروژه از آن به SQLite محلی مهاجرت کرده) وابسته بودند. مسیر وب‌اپ (`handle_image_upload`) از قبل یک فال‌بک محلی مخصوص خودش داشت؛ `upload_telegram_photo` — که برای عکس سیگنال‌های **تأییدشده‌ی ربات** صدا زده می‌شود (هم تأیید دستی ادمین، هم انتشار خودکار رول‌های Guardian+) — چنین فال‌بکی نداشت و در نبود کلید Supabase، بی‌صدا `None` برمی‌گرداند.

**اثر عملی:** تقریباً هر سیگنالی که از ربات با عکس تأیید می‌شد، در سایت بدون عکس نمایش داده می‌شد — بدون هیچ خطایی در لاگ.

**فیکس:** فال‌بک محلی از `routes.py` به یک نقطه‌ی مشترک (`image_upload._upload_bytes`) منتقل شد، تا هم `upload_telegram_photo` و هم `upload_web_image` خودکار از آن بهره ببرند. `routes.py` هم ساده‌تر شد (منطق تکراری حذف شد). با یک عکس جعلی تأیید شد: قبل از فیکس `None`، بعد از فیکس مسیر واقعی روی دیسک.

## ۲۵. مسیر فونت واترمارک اشتباه بود — واترمارک فارسی به‌صورت جعبه‌ی خالی

`services/watermark.py` با دو `".."` به‌جای سه‌تا، مسیر `Vazirmatn-Bold.ttf` را به `signal_bot/memeland_site/...` حساب می‌کرد — پوشه‌ای که اصلاً وجود ندارد (`memeland_site` کنار `signal_bot/` است، نه داخلش). چون فونت جایگزین (DejaVu) فارسی را پشتیبانی نمی‌کند، اگر ادمین با `/setwatermark` متن فارسی تنظیم کند، حروف به‌صورت جعبه‌ی خالی (tofu) روی هر عکس سیگنال نمایش داده می‌شدند.

**فیکس:** مسیر درست شد (سه `".."`)، چند فونت فارسی/عربی رایج لینوکسی (`Noto Naskh/Sans Arabic`) به‌عنوان کاندیدای اضافه اضافه شد، و یک لاگ هشدار اضافه شد که اگر واقعاً هیچ فونت فارسی پیدا نشود، در `/logs` دیده شود:

```
WTM_NoPersianFont: text='کانال من' — هیچ فونت فارسی/عربی پیدا نشد؛ ...
```

⚠️ **توجه مهم:** خود فایل `Vazirmatn-Bold.ttf` در پروژه وجود ندارد. مسیر کد الان درست است، ولی برای رندر صحیح فارسی باید این فایل (یک فونت رایگان و شناخته‌شده — [Vazirmatn روی Google Fonts](https://fonts.google.com/specimen/Vazirmatn)) را در `memeland_site/static/fonts/Vazirmatn-Bold.ttf` قرار دهید. تا آن زمان، اگر سرور شما `fonts-noto` نصب داشته باشد، به‌طور خودکار به آن فال‌بک می‌شود؛ در غیر این صورت لاگ هشدار بالا را می‌بینید.

## ۲۶. ناهماهنگی کادر بین دکمه‌ی چت و وب‌اپ

دکمه‌های `vip_add_`/`vip_remove_` داخل چت فقط دیتابیس ربات را می‌نوشتند (`staff_repo`)، در حالی‌که مسیر وب‌اپ (`add_or_update_staff`، از دور دوم) هر دو دیتابیس را می‌نویسد. اثر: کسی که با دکمه‌ی چت VIP Helper می‌شد در وب‌اپ درست دیده می‌شد (چون `get_staff_list` هر دو دیتابیس را می‌خواند)، ولی اگر بعداً از همان دکمه‌ی چت **حذف** می‌شد، ردیف دیتابیس سایت دست‌نخورده می‌ماند — یعنی دسترسی سمت سایت باقی می‌ماند.

**فیکس:** دو تابع کوچک (`sync_staff_row_from_bot`/`unsync_staff_row_from_bot`) به `site/auth.py` اضافه شد و از هر دو دکمه‌ی چت صدا زده می‌شود — بدون اینکه فیلد `added_by` (که فقط بات دارد) از بین برود.

## ۲۷. UTC در پرونده‌ی تریدر (سازگاری، نه باگ فعال)

`site/traders.py::get_trader_dossier` مثل `get_feed`، فیلد `created_at` را بدون نشانگر UTC برمی‌گرداند. فعلاً بی‌ضرر است چون `dossier.js` این فیلد را نمایش نمی‌دهد، ولی برای جلوگیری از یک غافلگیری بعدی (اگر روزی «۳ ساعت پیش» به این کارت اضافه شود)، همینجا هم با `_iso_utc()` هماهنگ شد.

## ۲۸. سخت‌سازی دفاعی در `check_entry_alerts`

`entry_price` در ستونی با affinity=REAL ذخیره می‌شود، ولی مقایسه‌ی `price <= entry` بدون `try/except` بود. اگر یک روز مقدار غیرعددی در این ستون بیفتد (دستی، یا یک مسیر ورودی آینده)، **کل حلقه برای همه‌ی سیگنال‌های آن اجرا** کرش می‌کرد، نه فقط یک ردیف — یعنی یک رکورد بد، آلارم قیمت را برای همه خاموش می‌کرد، هر ۴۵ ثانیه، تا کسی متوجه شود. حالا این مقدار قبل از مقایسه `float()` می‌شود؛ اگر نامعتبر بود، فقط همان ردیف لاگ و رد می‌شود.

## ۲۹. یافته‌ی مهم که باگ نیست — قابلیت هشدار قیمت عملاً خاموش است

بک‌اند «هشدار رسیدن به قیمت ورود» (`services/price_feed.py`, `get_alert_eligible_signals`, `check_entry_alerts`) کاملاً درست پیاده‌سازی و تست شده، ولی **هیچ رابط کاربری‌ای (نه وب‌اپ، نه ربات) وجود ندارد که `chain`، `entry_price`، یا `direction` را هنگام ثبت سیگنال پر کند** — فقط `contract_address` قابل ثبت است. تابع `detect_chain_and_price` هم برای تشخیص خودکار نوشته شده ولی هیچ‌جا صدا زده نمی‌شود.

این یک قابلیت نیمه‌تمام است که از قبل همین‌طور بوده، نه رگرسیونی از کار من. تصمیم با شماست:
- اگر می‌خواهید تکمیل شود، پیشنهاد: در فرم ثبت سیگنال وب‌اپ، وقتی `contract_address` پر شد، خودکار `detect_chain_and_price` صدا زده شود و قیمت لحظه‌ای به‌عنوان `entry_price` پیشنهاد شود (با امکان ویرایش دستی).
- اگر فعلاً لازم نیست، همین‌طور بی‌خطر می‌ماند (جاب هر ۴۵ ثانیه اجرا می‌شود ولی چیزی برای بررسی پیدا نمی‌کند).

## ۳۰. یافته‌ی معماری که باید بدانید — دو سطح دسترسی «ادمین»، نه یکی

در بررسی `handlers/admin.py` مشخص شد پروژه از ابتدا (نه از کار من) **دو سطح متفاوت** برای «ادمین» دارد، که هیچ‌جا صریح مستند نشده بود:

- **سطح ۱ — سوپرادمین (`ADMIN_IDS` از `.env`):** تنها کسانی که می‌توانند `/markpaid` (تسویه‌ی پاداش نقدی)، `/setwatermark`، تنظیم کیف پول سولانا، پیام همگانی (broadcast)، ویرایش دستی امتیاز، و پایان فصل را اجرا کنند.
- **سطح ۲ — ادمین/دستیار کادر (`staff` جدول، هر دو دیتابیس):** می‌توانند سیگنال تأیید/رد کنند، در وب‌اپ سیگنال حذف کنند، و کادر مدیریت کنند — ولی به دستورات مالی/تنظیمات حساس بالا دسترسی **ندارند** (بی‌صدا نادیده گرفته می‌شوند، بدون پیام خطا).

این یک تصمیم امنیتی معقول به‌نظر می‌رسد (دفاع لایه‌ای — دستورات حساس فقط از طریق تغییر `.env` و ری‌استارت قابل‌اعطا هستند، نه یک ردیف دیتابیس)، **ولی من آن را یک‌طرفه تغییر ندادم** چون تصمیم امنیتی است، نه باگ مشخص. اگر می‌خواهید کسی که از وب‌اپ «ادمین» شده به این دستورات هم دسترسی داشته باشد، باید صریح تصمیم بگیرید — و در آن صورت حداقل باید به‌جای نادیده‌گرفتن بی‌صدا، یک پیام خطای واضح («این دستور فقط برای مدیر ارشد است») نمایش داده شود.

## ۳۱. فایل‌های ممیزی‌شده در این دور — بدون باگ یافت‌شده

`web/ipn_server.py`، `services/ipn_signature.py`، `services/payments.py`، `handlers/payments.py`، `handlers/support.py`، `services/webapp_auth.py`، `services/access.py`، `services/scoring.py`، `services/alpha_score.py`، `services/price_feed.py`، `db/prize_repo.py`، `db/caller_donations_repo.py`، `db/staff_repo.py`، `db/users_repo.py`، `db/rewards_repo.py`، `site/kv.py`، `handlers/signals.py`، `handlers/common.py`، `handlers/text_router.py`، `handlers/profile.py`، `keyboards/keyboards.py`. این‌ها یا از قبل با دقت اصلاح شده بودند (نشانه‌های `SEC-02`/`SEC-03` در کامنت‌ها)، یا به‌اندازه‌ی کافی ساده و خودسازگار بودند که جای نگرانی نداشته باشند.

## ۳۲. اعتبارسنجی دور چهارم

`tests/test_round4_audit.py` — ۱۸ مورد، همه موفق:

```
۱  لینک معکوس سیگنال بومیِ ربات → سایت              ۳ مورد ✅
۲  اثر عملی: حذف سیگنال بومیِ ربات از سایت           ۲ مورد ✅
۳  آپلود عکس بدون Supabase (فال‌بک محلی)             ۴ مورد ✅
۴  مسیر فونت واترمارک + رندر متن فارسی               ۳ مورد ✅
۵  هماهنگی کادر بین دکمه‌ی چت و وب‌اپ                 ۲ مورد ✅
۶  UTC در پرونده‌ی تریدر                              ۳ مورد ✅
۷  سخت‌سازی check_entry_alerts                        ۱ مورد ✅
```

به‌علاوه سه مجموعه‌ی قبلی (۱۱۶ تست) بدون رگرسیون دوباره اجرا شدند.

**مجموع تجمعی چهار دور: ۱۳۴ تست خودکار، همه موفق.**

## ۳۳. دیپلوی دور چهارم

```bash
./APPLY.sh /path/to/MEMELAND-main
```

هیچ دستور جدیدی این دور اضافه نشد. بعد از دیپلوی معمول (`/wipe_sessions` اگر هنوز نزده‌اید، `/diag`، `/sync_all`)، یک سیگنال آزمایشی با عکس از داخل ربات تأیید کنید و مطمئن شوید در وب‌اپ عکسش دیده می‌شود — این ساده‌ترین راه تأیید فیکس شماره‌ی ۲۴ روی سرور واقعی است.

اگر می‌خواهید واترمارک فارسی درست رندر شود، فایل `Vazirmatn-Bold.ttf` را در `memeland_site/static/fonts/` قرار دهید (نگاه کنید به بند ۲۵).

---
---

# دور پنجم — فایل‌های حاشیه‌ای (پیکربندی، وابستگی‌ها، CSS، مستندات)

این دور روی چیزهایی تمرکز کرد که تا حالا باز نشده بودند: `requirements.txt`، `.env.example`، `README.md`، `CHANGELOG.md`، سه فایل CSS، و دو فایل JS که فقط export‌شان چک شده بود نه کل منطق‌شان (`avatars.js`, `pull_refresh.js`).

## ۳۴. بحرانی: نصب تازه‌ی پروژه اصلاً بالا نمی‌آید

`services/watermark.py` از `PIL` (Pillow) استفاده می‌کند، ولی **Pillow اصلاً در `requirements.txt` نبود**. زنجیره‌ی import در `main.py` این‌طور است:

```
main.py → handlers.admin → services.image_upload → services.watermark → PIL
```

این زنجیره در همان چند خط اول `main.py`، قبل از هر اتصالی به تلگرام، اجرا می‌شود. یعنی روی هر سرور تازه‌ای که دقیقاً طبق دستور خودِ `README.md` (`pip install -r requirements.txt`) نصب شود، ربات با این خطا کرش می‌کند:

```
ModuleNotFoundError: No module named 'PIL'
```

**این را در یک virtualenv کاملاً ایزوله (نه محیط آلوده‌ی من) تکرار کردم** تا مطمئن شوم واقعی است، نه یک فرض نظری:

```
--- بسته‌های نصب‌شده (باید Pillow نباشه) ---
✅ Pillow نصب نشده - دقیقاً طبق requirements.txt
--- تست ایمپورت واقعی در این محیط ایزوله ---
ModuleNotFoundError: No module named 'PIL'
```

**فیکس:** `Pillow>=10.0,<13.0` به `requirements.txt` اضافه شد، و در همان virtualenv ایزوله دوباره تست شد — این‌بار کل زنجیره‌ی import اصلیِ `main.py` (`admin`, `common`, `profile`, `signals`, `payments`, `support`, `ipn_server`, `jobs.scheduled`) با موفقیت بارگذاری شد.

⚠️ روی سرور فعلیِ شما این مشکلی ایجاد نکرده چون Pillow احتمالاً از قبل (به هر دلیلی، شاید نصب دستی یا وابستگیِ یه پکیج دیگه) روی محیطش هست. ولی هر بار که بخواهید روی یک سرور تازه دیپلوی کنید، یا محیط را rebuild کنید، بدون این فیکس دوباره گیر می‌کردید.

## ۳۵. باگ بصری واقعی: صفحه‌ی خواندن مقاله (Academy) عملاً بی‌استایل بود

با یک اسکریپت مقایسه‌ی خودکار (استخراج تمام کلاس‌های CSS تعریف‌شده در برابر تمام کلاس‌هایی که JS/HTML واقعاً استفاده می‌کنند)، ۱۸ کلاس «استفاده‌شده ولی تعریف‌نشده» پیدا شد. اکثرشان بی‌خطر بودند (استایل واقعی‌شان از طریق ارث‌بری CSS یا `style` inline تأمین می‌شد — با بررسی دستی تک‌تک تأیید شد، نه فرض).

ولی یکی واقعی بود: از ۷ کلاس خانواده‌ی `article-reader-*`، فقط `.article-reader-cover` (+ `img` داخلش) و `.article-read-badge` تعریف شده بودند؛ `.article-reader-container`، `.article-reader-header`، `.article-reader-title`، `.article-reader-meta`، و `.article-reader-body` — یعنی دقیقاً کانتینر، تیتر، متادیتا، و **متن اصلی مقاله** — هیچ تعریف CSS نداشتند و هیچ `style` inline ای هم جایگزینش نمی‌کرد. نتیجه: زیر یک تصویر کاور کاملاً پولیش‌شده، تیتر و متن مقاله با استایل خام مرورگر رندر می‌شدند — یک پرش بصری واضح در همان صفحه.

**فیکس:** پنج قانون CSS گمشده اضافه شد، با همان زبان بصری پروژه (رنگ‌ها/فاصله‌گذاری/تایپوگرافی از متغیرهای CSS موجود).

## ۳۶. نام‌گذاری گمراه‌کننده (بدون اثر عملی)

دکمه‌ی «حذف» در فهرست کادر، کلاس `btn-del-article` داشت — دقیقاً همان نامی که دکمه‌ی *واقعیِ* حذف مقاله (در بخش دیگری از کد) باید داشته باشد، ولی این دکمه در واقع `removeStaffAction` را صدا می‌زند، نه حذف مقاله را. چون این کلاس هیچ‌جا در CSS تعریف نشده بود (ظاهرش کاملاً از `style` inline می‌آمد)، **هیچ اثر بصری یا کارکردی نداشت** — فقط یک نام گمراه‌کننده برای هر توسعه‌دهنده‌ی بعدی. به `btn-del-staff` تغییر نام یافت.

## ۳۷. مستندات ناهماهنگ با کد واقعی

- `CHANGELOG.md` ادعا می‌کند «دسترسی‌پذیری (focus-visible + اندازه‌ی دکمه‌های کوچک)» در یک فاز قبلی انجام شده، ولی هیچ‌کدام در CSS فعلی پیدا نشد (نه `:focus-visible`، نه کلاس‌های `.mini-btn`/`.ba-btn`/`.admin-btn` که آن فاز ادعا می‌کند تغییرشان داده). یا این کار در نسخه‌ای دیگر انجام و هیچ‌وقت merge نشده، یا بعداً توسط یک بازنویسی (رفرش نام کلاس‌ها) بی‌آنکه دوباره اعمال شود، از بین رفته. **این را خودم تغییر ندادم** — چون تغییرات UI/UX جدید سزاوار تأیید جداگانه‌اند، نه یک فیکس ضمنی. در فازبندی زیر آورده شده.
- `README.md` به فایلی به نام `memeland-hub_UPDATED.html` اشاره می‌کند که دیگر وجود ندارد (الان `index.html` است — کد خودش این را درست مدیریت می‌کند، فقط مستندات قدیمی است).
- `TELEGRAM_BOT_USERNAME` و سیستم `ADMIN_PIN`/`CALLER_PIN`/`SUBSCRIBER_PIN`/`COMMUNITY_PIN`/`UNLOCK_PIN` — بازمانده‌ی سیستم قدیمیِ «Telegram Login Widget + پین» هستند که با ورود به معماری WebApp کاملاً منسوخ شده‌اند. بررسی کردم: **هیچ‌جای کد دیگری این پنج پین را نمی‌خواند** (فقط در `settings.py` تعریف می‌شوند، بی‌اثر). حذفشان از `.env.example` بی‌خطر بود و انجام شد؛ در خودِ `settings.py` دست‌نخورده باقی ماندند (حذف کامل‌شان ریسک بی‌مورد دارد برای یک فایده‌ی صرفاً آرایشی).
- `.env.example` فاقد `SITE_DB_FILE` و `IPN_WEBHOOK_HOST` بود — هر دو با مقدار پیش‌فرض درست اضافه شدند.

## ۳۸. فایل‌های ممیزی‌شده در این دور — بدون باگ کارکردی

`avatars.js` (کامل، از جمله بررسی دقیق مسیر `roleKey='super_admin'` که در ابتدا مشکوک به‌نظر می‌رسید ولی تأیید شد کاملاً عمدی و درست است)، `pull_refresh.js` (کامل)، `keyboards/keyboards.py` (کامل — همه‌ی ۱۲ دکمه‌ی پنل ادمین به هندلر واقعی وصل‌اند؛ الگوی `split("_", 2)` برای جلوگیری از ابهام کلیدهای حاوی زیرخط مثل `win_2x` از قبل درست پیاده‌سازی شده)، `utils.py`، `site/kv.py`.

## ۳۹. اعتبارسنجی دور پنجم

فیکس‌های این دور (Pillow، CSS، نام‌گذاری) ماهیتاً به تست یکپارچگی نیاز نداشتند (یکی سطح بسته‌بندی/import است، دیگری صرفاً بصری) — به‌جایش با روش‌های مستقیم‌تر تأیید شدند: virtualenv کاملاً ایزوله برای Pillow (نشان داده شده در بند ۳۴)، و بررسی تعادل `{`/`}` + شمارش دقیق کلاس‌های اضافه‌شده برای CSS. رگرسیون کامل روی هر چهار مجموعه‌ی تست موجود (۱۳۴ مورد) بدون شکست اجرا شد.

## ۴۰. دیپلوی دور پنجم

```bash
./APPLY.sh /path/to/MEMELAND-main
pip install -r signal_bot/requirements.txt   # حالا Pillow را هم نصب می‌کند
```

هیچ دستور ربات یا مهاجرت دیتابیسی این دور لازم نیست. تغییرات CSS بلافاصله با رفرش صفحه در وب‌اپ دیده می‌شوند.
