# طرح سیستم مدیریت اشتراک VIP

وضعیت: **طرح — پیاده‌سازی نشده.** این سند برای بررسی و تأیید قبل از کدنویسی است.

---

## ۱. دامنه

یک بخش مدیریتی برای گروه VIP که این‌ها را پوشش دهد:

- فهرست اعضا با تاریخ ثبت‌نام، تاریخ انقضا، نوع اشتراک
- افزودن، تمدید، تعلیق و حذف دستی توسط ادمین
- هشدار خودکار به کاربر، ۲ روز و ۱ روز قبل از انقضا
- اعمال خودکار انقضا (قطع دسترسی + حذف از گروه تلگرام)
- اتصال به پرداخت خودکار (NOWPayments) که از قبل موجود است

**خارج از دامنه‌ی این طرح:** سطوح چندگانه‌ی VIP با دسترسی‌های متفاوت، کد تخفیف،
بازپرداخت، و پرداخت تکرارشونده‌ی خودکار. ساختار طوری طراحی شده که بعداً بدون
تغییر schema اضافه شوند.

---

## ۲. یک تصمیم معماری که همه‌چیز به آن وابسته است

پروژه دو دیتابیس دارد: `signals.db` (ربات) و `site.db` (وب). **هر باگ جدی که در این
دو دور اصلاح کردیم، ریشه در همین دوگانگی داشت** — کادر در یکی نوشته می‌شد و از دیگری
خوانده، `users.role` در دیتابیسی خوانده می‌شد که جدولش آنجا نبود، و سیگنال‌ها اصلاً
سینک نمی‌شدند.

**اشتراک VIP نباید این الگو را تکرار کند.**

> **تصمیم:** جدول `vip_memberships` فقط و فقط در `site.db` ساخته می‌شود، و **تنها**
> از طریق ماژول `signal_bot/site/vip.py` خوانده و نوشته می‌شود. هیچ کد دیگری مستقیم
> به این جدول SQL نمی‌زند.

دلیل انتخاب `site.db`:

- کنترل دسترسی VIP از قبل آنجاست (`site/signals.py::_viewer_has_vip_access`)
- ربات از قبل `signal_bot.site.auth` را import می‌کند، پس دسترسی برایش رایگان است
- پس از پچ اخیر، `site.db` روی WAL است و تحمل خواندن هم‌زمان را دارد

هیچ ستون VIP در `signals.db` اضافه نمی‌شود. اگر روزی لازم شد، از تابع می‌آید نه از
جدول موازی.

---

## ۳. مدل داده

```sql
CREATE TABLE IF NOT EXISTS vip_memberships (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    telegram_id       INTEGER NOT NULL,
    plan_key          TEXT    NOT NULL,          -- 'monthly' | 'quarterly' | 'yearly' | 'lifetime' | 'trial'
    status            TEXT    NOT NULL DEFAULT 'active',
                                                 -- 'active' | 'expired' | 'revoked' | 'pending'
    started_at        TEXT    NOT NULL,          -- ISO-8601 UTC
    expires_at        TEXT,                      -- NULL فقط برای lifetime
    source            TEXT    NOT NULL DEFAULT 'manual',
                                                 -- 'manual' | 'payment' | 'gift' | 'migration'
    granted_by        INTEGER,                   -- کدام ادمین؛ NULL برای پرداخت خودکار
    order_id          TEXT,                      -- لینک به prize_pool/NOWPayments
    amount_usd        REAL,
    note              TEXT,
    tg_invite_link    TEXT,                      -- لینک تک‌مصرفِ صادرشده برای این عضویت
    joined_group_at   TEXT,                      -- کِی واقعاً وارد گروه شد
    removed_group_at  TEXT,
    created_at        TEXT NOT NULL,
    updated_at        TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_vip_tg      ON vip_memberships(telegram_id);
CREATE INDEX IF NOT EXISTS idx_vip_expiry  ON vip_memberships(status, expires_at);
```

```sql
-- دفتر رویدادها. جدا از جدول اصلی چون سؤال «چرا دسترسی این آدم قطع شد؟»
-- را فقط تاریخچه جواب می‌دهد، نه وضعیت فعلی.
CREATE TABLE IF NOT EXISTS vip_events (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    membership_id INTEGER NOT NULL REFERENCES vip_memberships(id) ON DELETE CASCADE,
    telegram_id   INTEGER NOT NULL,
    event         TEXT NOT NULL,   -- 'granted' | 'renewed' | 'warn_2d' | 'warn_1d'
                                   -- | 'expired' | 'revoked' | 'joined' | 'removed'
                                   -- | 'invite_issued' | 'notify_failed'
    detail        TEXT,
    actor         INTEGER,         -- ادمین، یا 0 برای سیستم
    created_at    TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_vip_events_lookup ON vip_events(membership_id, event);
```

### چرا چند ردیف به ازای هر کاربر، نه یک ردیف

یک ردیف به‌ازای هر *دوره‌ی* اشتراک، نه به‌ازای هر کاربر. تمدید یعنی ردیف جدید، نه
`UPDATE` روی قدیمی.

- تاریخچه‌ی خرید کاربر حفظ می‌شود (چند بار تمدید کرده، کِی قطع شده و برگشته)
- گزارش درآمد ماهانه بدون جدول جداگانه ممکن می‌شود
- «عضویت فعلی» یعنی: آخرین ردیف با `status='active'`

### چرا `expires_at` و نه `duration_days`

تاریخ مطلق ذخیره می‌شود، نه مدت. با مدت، هر بار باید از `started_at` حساب کنی و
تمدیدِ زودهنگام (قبل از انقضا) منطق پیچیده‌ای می‌خواهد. با تاریخ مطلق، تمدید فقط
یک محاسبه‌ی ساده است:

```
expires_at جدید = max(expires_at فعلی, اکنون) + مدت پلن
```

`max` مهم است: اگر کاربر ۵ روز مانده تمدید کند، آن ۵ روز باید حفظ شود، نه اینکه
از امروز شروع شود.

### پلن‌ها در `config/settings.py`

```python
VIP_PLANS = {
    "trial":     {"label": "🎁 آزمایشی",     "days": 7,    "price_usd": 0},
    "monthly":   {"label": "📅 یک‌ماهه",      "days": 30,   "price_usd": 25},
    "quarterly": {"label": "📅 سه‌ماهه",      "days": 90,   "price_usd": 65},
    "yearly":    {"label": "📅 یک‌ساله",      "days": 365,  "price_usd": 200},
    "lifetime":  {"label": "♾️ مادام‌العمر",  "days": None, "price_usd": 500},
}
VIP_WARN_DAYS = [2, 1]          # هشدارها؛ افزودن 7 کافی است برای هشدار یک‌هفته‌ای
VIP_GROUP_ID = os.environ.get("VIP_GROUP_ID", "").strip()
VIP_GRACE_HOURS = 6             # مهلت بعد از انقضا قبل از حذف از گروه
```

پلن‌ها در تنظیمات‌اند نه دیتابیس، چون تعدادشان کم است و تغییرشان یک دیپلوی است.
`plan_key` در ردیف ذخیره می‌شود، پس تغییر قیمت آینده روی عضویت‌های قدیمی اثر ندارد.

---

## ۴. چرخه‌ی حیات

```
                    ┌─────────┐
     پرداخت تأییدنشده │ pending │
                    └────┬────┘
    IPN تأیید شد / ادمین  │
     دستی اعطا کرد        ▼
                    ┌─────────┐
                    │ active  │◄──── تمدید (expires_at جلو می‌رود،
                    └────┬────┘      ردیف جدید، وضعیت active می‌ماند)
                         │
         ┌───────────────┼───────────────┐
         │               │               │
  expires_at رسید    ادمین لغو کرد    پرداخت برگشت خورد
         ▼               ▼               ▼
    ┌─────────┐    ┌─────────┐    ┌─────────┐
    │ expired │    │ revoked │    │ revoked │
    └─────────┘    └─────────┘    └─────────┘
```

`pending` فقط برای پرداختی که فاکتور صادر شده ولی هنوز تأیید نشده. دسترسی نمی‌دهد.

---

## ۵. جاب زمان‌بندی — نقطه‌ای که باید دقیق باشد

یک جاب، هر ساعت (`JobQueue.run_repeating(interval=3600)`)، سه کار:

```
برای هر عضویت با status='active':
    ۱. اگر expires_at گذشته + VIP_GRACE_HOURS  →  انقضا
    ۲. وگرنه اگر روزهای باقی‌مانده در VIP_WARN_DAYS  →  هشدار
    ۳. وگرنه هیچ
```

### ⚠️ مسئله‌ی اصلی: هشدار تکراری

جاب هر ساعت اجرا می‌شود. «۲ روز مانده» برای ۲۴ ساعت متوالی درست است. بدون محافظت،
کاربر **۲۴ پیام یکسان** می‌گیرد.

این دقیقاً همان کلاس باگی است که در پروژه با `entry_alert_sent` حل شده. اما آنجا یک
ستون بولین کافی بود چون آلارم یک‌بار است؛ اینجا چند هشدار مختلف داریم.

**راه‌حل: جدول `vip_events` به‌عنوان قفل idempotency.**

```python
def _already_warned(membership_id: int, days: int) -> bool:
    # یک SELECT روی ایندکس idx_vip_events_lookup
    return exists(vip_events WHERE membership_id=? AND event=f'warn_{days}d')
```

قبل از هر هشدار این چک می‌شود؛ بعد از ارسال موفق، رویداد ثبت می‌شود. چون `event`
شامل تعداد روز است، هشدار ۲روزه و ۱روزه مستقل‌اند و هر کدام دقیقاً یک‌بار می‌روند.

**ترتیب حیاتی:** اول پیام بفرست، بعد رویداد را ثبت کن. برعکسش یعنی اگر ارسال
شکست بخورد، هشدار برای همیشه گم می‌شود. اگر ارسال شکست خورد، `notify_failed` ثبت
می‌شود (که قفل نیست) تا دفعه‌ی بعد دوباره تلاش شود.

### محاسبه‌ی روزهای باقی‌مانده

```python
remaining = (expires_at - utcnow()).total_seconds() / 86400
bucket = math.ceil(remaining)     # 1.3 روز → سطل «۲ روز»
```

`ceil` یعنی کاربری که ۱.۳ روز مانده، هشدار «۲ روز» را می‌گیرد. این درست است: بهتر
است زودتر هشدار بدهیم تا اینکه پنجره را از دست بدهیم.

### مهلت ۶ ساعته

`VIP_GRACE_HOURS` جلوی حذف کسی را می‌گیرد که همین الان دارد پرداخت می‌کند و IPN
هنوز نرسیده. بدون آن، یک کاربر واقعی در لحظه‌ی انقضا از گروه بیرون می‌افتد و بعد
باید دوباره اضافه شود — تجربه‌ی بدی است و پشتیبانی می‌طلبد.

---

## ۶. اتصال به گروه تلگرام

### ورود

وقتی عضویت `active` شد:

1. `bot.create_chat_invite_link(VIP_GROUP_ID, member_limit=1, expire_date=now+48h)`
2. لینک در `tg_invite_link` ذخیره می‌شود
3. به کاربر پیام خصوصی می‌رود

`member_limit=1` حیاتی است — بدون آن لینک قابل فوروارد است و کل پی‌وال دور می‌خورد.
گروه باید روی «فقط با لینک دعوت» باشد، نه عمومی.

### خروج

وقتی `expired` یا `revoked` شد:

```python
await bot.ban_chat_member(VIP_GROUP_ID, telegram_id)
await bot.unban_chat_member(VIP_GROUP_ID, telegram_id, only_if_banned=True)
```

الگوی ban-then-unban استاندارد تلگرام برای «بیرون کن ولی بگذار بعداً برگردد» است.
فقط `ban` یعنی حتی با اشتراک جدید هم نمی‌تواند وارد شود.

### پیگیری ورود واقعی

هندلر `ChatMemberHandler` روی `VIP_GROUP_ID`: وقتی کسی وارد شد، `joined_group_at`
ست می‌شود. این تفاوت «لینک گرفت» و «واقعاً وارد شد» را روشن می‌کند — که برای
پشتیبانی («لینک را زدم کار نکرد») تنها داده‌ی مفید است.

### ⚠️ محدودیت

اگر کاربر در گروه باشد ولی ربات ادمین نباشد، `ban_chat_member` خطا می‌دهد و **عضویت
منقضی می‌شود ولی شخص در گروه می‌ماند**. این را باید در `/diag` صریح چک کرد:

```
• ربات در گروه VIP ادمین است: ✅/❌
```

بدون این چک، شکست کاملاً بی‌صداست — همان الگویی که در این پروژه بارها دیدیم.

---

## ۷. رابط ادمین

### ۷.۱ وب‌اپ — تب جدید در پنل پروفایل

دکمه‌ی سوم کنار «ثبت سیگنال» و «مدیریت کادر»: **👑 مدیریت VIP**

مودالی با سه بخش:

**الف) نوار خلاصه**

```
┌──────────────────────────────────────────┐
│  فعال: ۴۲   │  ۷ روز آینده منقضی: ۵   │
│  منقضی این ماه: ۸  │  درآمد ماه: ۷۲۵$  │
└──────────────────────────────────────────┘
```

**ب) اعطا یا تمدید**

```
شناسه عددی تلگرام:  [____________]
نوع اشتراک:         [▼ یک‌ماهه (۳۰ روز)]
یادداشت:            [____________]
                    [ اعطا / تمدید ]
```

وقتی شناسه وارد شد، اگر عضویت فعالی دارد، زیر فیلد نشان داده شود:

> ⚠️ این کاربر تا ۱۲ مهر اشتراک فعال دارد. با تمدید، ۳۰ روز به همان اضافه می‌شود
> (انقضای جدید: ۱۱ آبان).

این پیش‌نمایش جلوی اشتباه رایج «فکر کردم از امروز شروع می‌شود» را می‌گیرد.

**ج) فهرست اعضا** با چیپ‌های فیلتر — دقیقاً الگوی مودال کادر که الان کار می‌کند:

```
[ همه ]  [ فعال ]  [ نزدیک انقضا ]  [ منقضی ]

┌────────────────────────────────────────────┐
│ 👤 مهدی            @mehdi · 2088114041     │
│ 📅 یک‌ماهه · از ۱۲ شهریور تا ۱۲ مهر        │
│ ⏳ ۱۸ روز مانده          [تمدید] [لغو]     │
└────────────────────────────────────────────┘
┌────────────────────────────────────────────┐
│ 👤 علی              @ali · 123456789       │
│ 📅 سه‌ماهه · از ۱ مرداد تا ۱ آبان          │
│ 🔴 ۱ روز مانده  · هشدارها: ۲روزه ✓         │
│                          [تمدید] [لغو]     │
└────────────────────────────────────────────┘
```

نمایش «هشدارها: ۲روزه ✓» از `vip_events` می‌آید و مستقیم جواب می‌دهد به «آیا به
این کاربر خبر داده شده؟» — بدون باز کردن لاگ.

### ۷.۲ ربات — دستورات

```
/vip                    خلاصه‌ی وضعیت + ۵ نفر نزدیک انقضا
/vip list [فیلتر]       فهرست (active | expiring | expired)
/vip add <id> <plan>    اعطا یا تمدید
/vip info <id>          پرونده‌ی کامل یک نفر + تاریخچه‌ی رویدادها
/vip revoke <id>        لغو فوری
/vip run                اجرای دستی جاب (برای تست، idempotent است)
```

`/vip run` مهم است: بدون آن، تست کردن مسیر هشدار یعنی صبر کردن تا فردا.

### ۷.۳ اندپوینت‌ها

| متد | مسیر | دسترسی |
|---|---|---|
| `GET` | `/site/vip` | ادمینِ **اثبات‌شده** |
| `GET` | `/site/vip/{telegram_id}` | ادمینِ اثبات‌شده |
| `POST` | `/site/vip` | ادمینِ اثبات‌شده |
| `DELETE` | `/site/vip/{telegram_id}` | ادمینِ اثبات‌شده |
| `GET` | `/site/vip/me` | هر کاربر اثبات‌شده (فقط عضویت خودش) |

همه از `_require_admin()` رد می‌شوند که بعد از پچ اخیر هویت اثبات‌شده لازم دارد.
`/site/vip/me` عمداً جداست تا کاربر عادی بتواند وضعیت خودش را ببیند بدون دسترسی به
فهرست بقیه.

---

## ۸. اتصال به پرداخت

مسیر IPN از قبل موجود است و `order_id` را با پیشوند مسیریابی می‌کند
(`pool_`, `support_`). پیشوند سوم اضافه می‌شود: `vip_`.

```
کاربر پلن را انتخاب می‌کند
   └─> فاکتور NOWPayments با order_id = f"vip_{uid}_{plan}_{timestamp}"
          └─> ردیف vip_memberships با status='pending'
                 └─> IPN تأیید می‌شود (امضا بررسی می‌شود — مسیر موجود)
                        └─> status='active' + محاسبه‌ی expires_at
                               └─> لینک دعوت صادر و ارسال می‌شود
```

⚠️ **ابتدا مسیر IPN باید ممیزی شود.** در گزارش قبلی گفتم که بررسی نکرده‌ام. وصل
کردن اشتراک VIP به مسیری که ممیزی نشده، یعنی اگر آنجا ضعفی باشد، به اشتراک رایگان
تبدیل می‌شود. **پیشنهاد: پرداخت خودکار فاز آخر باشد؛ فازهای اول با اعطای دستی
ادمین کار کنند.**

---

## ۹. اتصال به کنترل دسترسی موجود

`site/signals.py` از قبل این را دارد:

```python
def _viewer_has_vip_access(viewer, owner_telegram_id) -> bool:
    if viewer and viewer.get("role") in ("admin", "caller", "subscriber", "community"):
        return True
    ...
```

مقادیر `subscriber` و `community` بازمانده‌ی سیستم PIN حذف‌شده‌اند و **هیچ‌وقت
ست نمی‌شوند** — سشن‌ها فقط `admin` یا `member` می‌گیرند. یعنی در عمل محتوای VIP
فقط برای ادمین باز است.

تغییر لازم:

```python
def _viewer_has_vip_access(viewer, owner_telegram_id) -> bool:
    if not viewer:
        return False
    if viewer.get("is_admin") or viewer.get("is_super_admin"):
        return True
    if viewer.get("vip_active"):          # ← جدید
        return True
    return owner_telegram_id is not None and owner_telegram_id == viewer.get("telegram_id")
```

`vip_active` در `auth.get_session()` و `authenticate_webapp()` پر می‌شود، با یک
فراخوانی `vip.is_active(telegram_id)`.

⚠️ **مثل `elevate=False`، این فیلد هرگز نباید در مسیر تأییدنشده ست شود** — وگرنه
یک هدر جعلی، اشتراک رایگان می‌دهد. همان کلاس باگی که در `quota.is_admin` بسته شد.

---

## ۱۰. لاگ و تشخیص

مطابق الگویی که الان در پروژه جا افتاده — یک خط، کلید=مقدار، با سطح درست:

```
VipGrant:  uid=2088114041 plan=monthly until=2026-10-20 by=admin src=manual
VipRenew:  uid=2088114041 plan=monthly old=2026-10-20 new=2026-11-19 by=admin
VipWarn:   uid=2088114041 days=2 sent=ok mid=17
VipExpire: uid=2088114041 mid=17 grace=6h removed=ok
VipJobRun: checked=42 warned2=3 warned1=1 expired=2 failed=0 ms=340
```

خط `VipJobRun` یک بار در هر اجرای جاب — نه یک خط به ازای هر کاربر. با ۴۲ عضو و
اجرای ساعتی، لاگ‌نویسی به ازای هر کاربر یعنی روزی **۱۰۰۸ خط** که همه‌چیز دیگر را
دفن می‌کند. فقط رویدادهای واقعی (هشدار، انقضا، شکست) خط جدا می‌گیرند.

شکست‌ها **همیشه** سطح `ERROR` می‌گیرند تا `/logs err` پیدایشان کند:

```
VipNotifyFail: uid=... err="Forbidden: bot was blocked by the user"
VipKickFail:   uid=... err="not enough rights"
```

اولی طبیعی است (کاربر ربات را بلاک کرده) و باید در پرونده‌ی کاربر دیده شود. دومی
خطای پیکربندی است و فوری باید اصلاح شود.

افزوده به `/diag`:

```
👑 VIP
• فعال: 42 | منقضی: 8
• ۷ روز آینده منقضی: 5
• ربات در گروه VIP ادمین است: ✅
• آخرین اجرای جاب: ۴۲ دقیقه پیش (checked=42)
```

---

## ۱۱. مواردی که باید صریح تصمیم‌گیری شوند

| مورد | رفتار پیشنهادی | چرا |
|---|---|---|
| کاربر ربات را بلاک کرده | `notify_failed` ثبت شود، انقضا طبق برنامه اجرا شود | نمی‌توان خبر داد، ولی اشتراک بی‌نهایت نمی‌شود |
| تمدید قبل از انقضا | `max(expires_at, now) + days` | روزهای باقی‌مانده نباید بسوزد |
| تمدید بعد از انقضا | از `now` شروع شود | دوره‌ی قطعی نباید جبران شود |
| دو IPN برای یک `order_id` | فقط اولی اعمال شود (چک `status != 'active'`) | همان الگوی idempotency که در `_handle_pool_payment` هست |
| کاربر دستی از گروه خارج شد | عضویت فعال بماند، `removed_group_at` ست شود | پولش را داده؛ لینک دوباره بگیرد |
| ادمین خودش | دسترسی VIP از `is_admin` بیاید نه از عضویت | ادمین لازم نیست عضویت بخرد |
| `VIP_GROUP_ID` ست نشده | همه‌چیز جز عملیات گروه کار کند، در `/diag` هشدار | نباید کل فیچر به‌خاطر یک تنظیم نداشته بخوابد |
| ساعت | همه‌جا UTC، نمایش با تبدیل | تجربه‌ی `datetime.now()` در لیدربورد |

---

## ۱۲. فازبندی

هر فاز مستقل قابل دیپلوی و تست است.

**فاز ۱ — هسته (بدون تلگرام، بدون پرداخت)**
`site/vip.py` + schema + اعطا/تمدید/لغو دستی + `GET /site/vip` + مودال وب‌اپ.
قابل آزمایش بدون هیچ گروهی.

**فاز ۲ — هشدارها**
جاب ساعتی + `vip_events` + قفل idempotency + `/vip run` برای تست دستی.
در این فاز هنوز کسی از گروه حذف نمی‌شود — فقط پیام می‌رود و `expired` ثبت می‌شود.
اجازه می‌دهد منطق زمان‌بندی را بدون ریسک بیرون انداختن مشتری واقعی تست کنید.

**فاز ۳ — گروه تلگرام**
لینک تک‌مصرف + ban/unban + `ChatMemberHandler` + چک ادمین بودن ربات در `/diag`.

**فاز ۴ — پی‌وال محتوا**
`vip_active` در سشن + اصلاح `_viewer_has_vip_access`.

**فاز ۵ — پرداخت خودکار**
پیشوند `vip_` در IPN. **فقط بعد از ممیزی مسیر IPN.**

---

## ۱۳. فایل‌های جدید و تغییریافته

| فایل | کار |
|---|---|
| `signal_bot/site/vip.py` | **جدید** — تمام منطق: CRUD، محاسبه‌ی انقضا، قفل رویداد |
| `signal_bot/site/db.py` | دو جدول جدید در `init_db()` |
| `signal_bot/config/settings.py` | `VIP_PLANS`, `VIP_WARN_DAYS`, `VIP_GROUP_ID`, `VIP_GRACE_HOURS` |
| `signal_bot/jobs/scheduled.py` | `vip_expiry_job` |
| `signal_bot/handlers/vip.py` | **جدید** — دستورات `/vip` |
| `signal_bot/site/routes.py` | پنج اندپوینت + ثبتشان |
| `signal_bot/site/auth.py` | `vip_active` در سشن (فقط مسیر اثبات‌شده) |
| `signal_bot/site/signals.py` | `_viewer_has_vip_access` |
| `memeland_site/static/js/modals.js` | `openVipModal` و توابع مرتبط |
| `memeland_site/index.html` | دکمه‌ی «👑 مدیریت VIP» |
| `signal_bot/main.py` | ثبت هندلرها و جاب + بخش VIP در `/diag` |

تخمین: حدود ۹۰۰ تا ۱۲۰۰ خط، که بیشترش در `vip.py` و `modals.js` است.

---

## ۱۴. آنچه قبل از شروع باید بدانم

۱. گروه VIP الان وجود دارد یا باید ساخته شود؟ اگر وجود دارد، چند عضو دارد و آیا
   لیست‌شان با تاریخ ثبت‌نام جایی هست که مهاجرت کنیم؟
۲. قیمت‌ها و مدت پلن‌ها چیست؟ (جدول بالا نمونه است)
۳. اشتراک VIP فقط دسترسی به گروه تلگرام است، یا محتوای `tier='vip'` در وب‌اپ را
   هم باز می‌کند؟ (فاز ۴ به این بستگی دارد)
۴. دوره‌ی آزمایشی می‌خواهید؟ اگر بله، خودکار یا فقط با اعطای ادمین؟
