# نقشه‌راه — اقدامات پیش‌رو

وضعیت این سند: زنده — هر بار که یک فاز تمام شد یا تصمیمی گرفته شد، به‌روزش کنید.

این نقشه‌راه نتیجه‌ی پنج دور ممیزی کامل کدبیس MEMELAND است (`REPORT.md` جزئیات فنی هر
دور را دارد). اینجا فقط **چه چیزی، به چه ترتیبی، و با چه تصمیمی** آمده — بدون تکرار
جزئیات فنی که در گزارش هست.

---

## فاز ۰ — کاری که تا این لحظه انجام شده (فقط برای مرجع)

| دور | موضوع اصلی |
|---|---|
| ۱ | تشخیص ادمین در وب‌اپ، ارتقای دسترسی امنیتی، سیستم لاگ یکپارچه |
| ۲ | سینک دوطرفه‌ی سیگنال‌ها، فهرست کادر، فیلترهای نمایش |
| ۳ | دیالوگ تأیید حذف (باگ Telegram Mini App)، هماهنگی UTC، خودترمیمی سینک |
| ۴ | لینک معکوس سیگنال‌های بومی، آپلود عکس بدون Supabase، فونت واترمارک |
| ۵ | نصب تازه (Pillow گمشده)، CSS ناقص صفحه‌ی مقاله، مستندات ناهماهنگ |

**۱۳۴ تست خودکار** (`signal_bot/tests/`) این‌ها را پوشش می‌دهند. قبل از هر تغییر بزرگ
بعدی، این‌ها را اجرا کنید:

```bash
cd signal_bot && for t in tests/test_*.py; do python3 "$t"; done
```

---

## فاز ۱ — اقدامات فوری (اگر هنوز انجام نداده‌اید)

این‌ها یک‌بار انجام می‌شوند و ریسک ندارند:

- [ ] `pip install -r signal_bot/requirements.txt` (حالا Pillow را هم نصب می‌کند)
- [ ] `/wipe_sessions confirm` در ربات — اگر از دور اول این کار را نکرده‌اید
- [ ] `/sync_all` سپس `/sync_all apply` — همگام‌سازی سیگنال‌های قدیمی
- [ ] `/diag` — باید همه‌چیز ✅ باشد
- [ ] فایل `Vazirmatn-Bold.ttf` را در `memeland_site/static/fonts/` بگذارید (برای رندر
      درست واترمارک فارسی — خود فونت از قبل در سایت شما لود می‌شود، فقط کپی‌اش کنید)
- [ ] یک سیگنال آزمایشی با عکس از **داخل ربات** تأیید کنید و مطمئن شوید در وب‌اپ عکسش
      دیده می‌شود (تأیید عملی فیکس دور ۴)

---

## فاز ۲ — تصمیم‌هایی که فقط شما می‌توانید بگیرید

این‌ها را عمداً خودم یک‌طرفه تغییر ندادم — یا امنیتی‌اند، یا محصولی، یا سلیقه‌ای.

### ۲.۱ دو سطح «ادمین» — نگه دارید یا یکی کنید؟

الان دستورات مالی/حساس ربات (`/markpaid`, `/setwatermark`, پیام همگانی, ویرایش
امتیاز, پایان فصل) فقط برای `ADMIN_IDS` داخل `.env` کار می‌کنند — نه هر کسی که از
وب‌اپ «ادمین» شده.

**گزینه‌ی الف (پیشنهاد من):** همین‌طور نگه دارید. دفاع لایه‌ای معقولی است — بالاترین
سطح دسترسی فقط با تغییر `.env` و ری‌استارت قابل‌تغییر باشد، نه یک ردیف دیتابیس.
فقط باید یک پیام خطای واضح اضافه شود (الان بی‌صدا رد می‌شود).

**گزینه‌ی ب:** کسی که از وب‌اپ «ادمین» شد به همه‌چیز دسترسی داشته باشد. اگر این را
می‌خواهید، بگویید — تغییرش کوچک است (چند خط در `handlers/admin.py`).

### ۲.۲ قابلیت هشدار قیمت — تکمیل یا رها؟

بک‌اند (`price_feed.py`, `check_entry_alerts`) کامل و تست‌شده است، ولی هیچ UI ای
`entry_price`/`chain` را پر نمی‌کند.

**طرح کامل این بخش — با جزئیات فنی، UI/UX، و سؤالات باز — در `PRICE_TRACKING_DESIGN.md`
آماده است.** خلاصه‌ی تصمیم‌هایی که آنجا باید بگیرید: سقف زمانی پیگیری قیمت (پیشنهاد:
۷ روز یا تا بسته‌شدن نتیجه، هرکدام زودتر)، بازه‌ی آپدیت (پیشنهاد: هر ۳-۵ دقیقه)، و
اینکه آیا کالرهای داخل ربات هم بتوانند آدرس کانترکت بدهند یا فعلاً فقط وب‌اپ کافی است.

### ۲.۳ بازبینی UI/UX نیمه‌کاره — دوباره انجام شود؟

`CHANGELOG.md` پروژه ادعا می‌کند «دسترسی‌پذیری» (focus-visible + دکمه‌های بزرگ‌تر
برای لمس) قبلاً انجام شده، ولی در کد فعلی وجود ندارد (جزئیات: بند ۳۷ گزارش). دو
مورد دیگر هم از قبل در برنامه بودند و انجام نشدند:

- تفکیک رنگی گرادیان اکشن‌های VIP از اکشن‌های عادی
- گرافیک SVG مینیمال (لوگو، حالت خالی، قفل VIP، بج رتبه) به‌جای ایموجی

اگر می‌خواهید این‌ها را (دوباره) انجام دهم، بگویید — این‌ها صرفاً بصری‌اند، ریسک
کارکردی ندارند، ولی چون سلیقه‌ای‌اند بهتر است با تأیید صریح شما پیش بروند.

---

## فاز ۳ — سیستم مدیریت اشتراک VIP (طرح آماده، پیاده‌سازی نشده)

طرح کامل در `VIP_DESIGN.md` است. خلاصه‌ی فازبندی خودش (که مستقل از این نقشه‌راه است):

| زیرفاز | محتوا | وابستگی |
|---|---|---|
| ۳.۱ | هسته: جدول عضویت، اعطا/تمدید/لغو دستی، مودال وب‌اپ | هیچ‌کدام |
| ۳.۲ | هشدارهای ۲روزه/۱روزه با جاب ساعتی + قفل idempotency | ۳.۱ |
| ۳.۳ | اتصال به گروه تلگرام (لینک دعوت، ban/unban خودکار) | ۳.۲ |
| ۳.۴ | پی‌وال محتوای VIP در وب‌اپ | ۳.۱ |
| ۳.۵ | پرداخت خودکار (اتصال به مسیر IPN موجود) | ۳.۱ + ممیزی IPN |

**پیش‌نیاز فاز ۳.۵:** مسیر IPN از نظر امنیتی درست به‌نظر می‌رسد (ممیزی شد، بند ۳۱
گزارش دور چهارم)، ولی قبل از وصل‌کردن یک قابلیت پولی جدید به آن، یک بار دیگر مرور
کنید — چهار سؤال باز در انتهای `VIP_DESIGN.md` (بند ۱۴) هست که باید جواب داده شوند
تا پیاده‌سازی شروع شود.

**توصیه‌ی من برای شروع:** زیرفاز ۳.۱ به‌تنهایی یک قابلیت کامل و قابل‌استفاده است
(اعطای دستی VIP، بدون گروه تلگرام یا هشدار خودکار). می‌شود همین را اول ساخت، دید
جواب می‌دهد، بعد زیرفازهای بعدی را اضافه کرد.

---

## فاز ۴ — تکمیل هشدار/ردیابی قیمت (فقط اگر در فاز ۲.۲ تصمیم گرفتید)

طرح کامل و فازبندی دقیق (۵.۱ تا ۵.۴) در `PRICE_TRACKING_DESIGN.md` است — شامل
طراحی UI، ستون‌های دیتابیس جدید، و منطق انقضای آپدیت. خلاصه:

۱. دو ستون جدید (`current_price`, `price_updated_at`) + گرفتن قیمت ورود به‌صورت
   async هنگام ثبت سیگنال (بدون معطل‌کردن دکمه‌ی ثبت).
۲. گسترش جاب موجود (`check_entry_alerts`) برای آپدیت دوره‌ای + توقف بعد از سقف
   زمانی یا بسته‌شدن نتیجه.
۳. نمایش در UI: بج زنده در کارت لیست (جایگزین «در حال معامله») + پنل قیمت در
   جزئیات سیگنال.
۴. تست خودکار برای این مسیر اضافه شود.

تخمین: ۳-۴ ساعت.

---

## فاز ۵ — نظافت کد (کم‌اولویت، بدون فوریت)

هیچ‌کدام از این‌ها باگ فعال نیستند — فقط باقی‌مانده‌های تمیزکاری:

- حذف پنج متغیر پین (`ADMIN_PIN` و بقیه) و `TELEGRAM_BOT_USERNAME` از
  `signal_bot/config/settings.py` — کاملاً بی‌اثرند (تأیید شد، هیچ‌جای دیگر خوانده
  نمی‌شوند)، فقط برای تمیزی. از `.env.example` قبلاً حذف شدند.
- سه دستور همپوشان: `/sync_report` (قدیمی، فقط یک‌طرفه)، `/resync_signal` (قدیمی،
  تک‌سیگنالی)، `/sync_all` (جدید، دوطرفه). هر سه کار می‌کنند، ولی می‌شود دو تای اول
  را در `/sync_all` ادغام کرد تا فقط یک دستور برای این کار بماند.
- به‌روزرسانی `README.md`: اصلاح نام فایل قدیمی (`memeland-hub_UPDATED.html` →
  `index.html`) و حذف اشاره به «Login Widget» که دیگر وجود ندارد.
- کلاس‌های SVG بلااستفاده در لوگوی قورباغه (`frog-eyelid` و مشابه) — نه در CSS نه در
  JS استفاده می‌شوند؛ صرفاً نام‌گذاری‌اند، بی‌خطر، پاک‌سازی اختیاری.

---

## فاز ۶ — نگهداری مستمر (توصیه، نه یک‌بار)

- **پاک‌سازی دوره‌ای `/static/uploads`:** از دور ۴، عکس سیگنال‌های تأییدشده در ربات
  هم روی دیسک محلی ذخیره می‌شوند (قبلاً فقط عکس‌های آپلودی وب‌اپ بودند). این یعنی
  رشد این پوشه سریع‌تر از قبل است. یک کرون‌جاب ساده (مثلاً حذف فایل‌های قدیمی‌تر از
  ۹۰ روز) یا مهاجرت به یک object storage واقعی (S3-سازگار) را در نظر بگیرید.
- **اجرای دوره‌ای `/diag`:** حالا وضعیت سینک، کادر، و لاگ را یک‌جا نشان می‌دهد —
  می‌تواند بخشی از چک هفتگی شما باشد.
- **نگه‌داشتن مجموعه‌ی تست:** هر بار که کد را دستی یا با کمک AI دیگری تغییر دادید،
  `tests/*.py` را اجرا کنید. این‌ها دقیقاً همان باگ‌هایی را می‌گیرند که پنج دور
  طول کشید پیدا شوند.

---

## ترتیب پیشنهادی

اگر بخواهید یک مسیر خطی داشته باشید:

```
فاز ۱ (فوری)
   │
   ▼
فاز ۲ (تصمیم‌ها) ──┬──► فاز ۴ (اگر ۲.۲ = تکمیل)
                   │
                   └──► فاز ۵ (وقت آزاد داشتید)
   │
   ▼
فاز ۳.۱ (هسته‌ی VIP) ──► ۳.۲ ──► ۳.۳ / ۳.۴ (موازی) ──► ۳.۵ (آخر از همه)
   │
   ▼
فاز ۶ (همیشگی، از همین الان شروع می‌شود)
```
